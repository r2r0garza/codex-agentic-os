# Automation Memory

- Run: 2026-07-14T12:35:51Z — implementation run (scheduled).
- Active milestone: Sprint 17 "Read-only operator HTTP API" (#17). Selected #106 (priority:2), the sole unblocked `agent-ready` issue; its sole dependency #105 was closed.
- Completed: added `GET /api/v1/runs/{run_id}/approvals` and `GET /api/v1/runs/{run_id}/usage`. Moved `_approval_payload` and `_usage_payload` from `cli.py` into shared `payloads.py`, so CLI and HTTP responses remain identical. Approvals preserve sanitized execution kind and known requester/decider attribution. Usage preserves provider-step order, served/requested model selection, normalized evidence, explicit unavailable-usage reasons, and the run aggregate without fabricated zero counts. Both routes return structured unknown-run `404` responses, use the read-only state store, and introduce no mutation route. Added Plan 0099 and DEVELOPMENT.md route guidance.
- Verification: activated `.venv`; focused `tests/test_api.py` suite `33 passed` (up from 28, +5 net); full suite `705 passed` (up from 700, +5 net); rebuilt and checked the index (27 files, 1057 symbols, 6256 relationships); `git diff --check` clean. Focused live loopback tests proved shared-builder and CLI parity, approved provider attribution, available/unavailable usage aggregation, structured errors, request-content redaction, and unchanged run/step/history snapshots after both reads.
- Durable state: implementation commit `c916226` pushed to `origin/main`; #106 closed with commit and verification evidence.
- Blocked review: #107's dependencies #105/#106 are closed, so `blocked` was removed and `agent-ready` added with an evidence comment. #108 remains correctly `blocked` on open #107. Sprint 17 now has one ready issue: #107 (priority:2).
- Retrospective state: Sprint 17 still has open issues #107/#108, so close-or-remediate retrospective review is not yet eligible.
- Roadmap horizon: 12 ordered open milestones before and after this run (Sprint 17 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was created.
- Next eligible action: implement #107 (harden the complete read-only HTTP API redaction and structured-error guarantees) in Sprint 17. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.

---

- Run: 2026-07-14T12:34:00Z — implementation run (scheduled).
- Active milestone: Sprint 17 "Read-only operator HTTP API" (#17). At start it already had four open issues (#105-#108) from an unrecorded prior replenishment; selected #105 (priority:1, agent-ready, no stated dependency).
- Completed: added `codex-agentic-os api serve --host HOST --port PORT --state-db PATH`, a loopback-only, read-only HTTP server exposing `GET /api/v1/runs`, `GET /api/v1/runs/{run_id}`, and `GET /api/v1/runs/{run_id}/history`. `is_loopback_bind_host` accepts only literal loopback IPs (`127.0.0.1`, `::1`, any `127.0.0.0/8` literal), rejecting hostnames like `localhost` and any non-loopback address before a socket is ever opened; the state database is opened read-only. Every other path returns a structured `404`; every non-GET method returns a structured `405` with an `Allow: GET` header; there is no mutation route of any kind. Extracted `_run_payload`, `_step_payload`, `_run_list_payload`, `_history_payload`, `_artifact_record_payload` out of `cli.py` into a new `payloads.py` (no behavior change) so the HTTP API reuses the exact CLI JSON contracts instead of inventing divergent shapes, with no circular import (api.py depends only on payloads.py/runtime.py; cli.py imports api.py at module load). Reused the worker command's SIGINT/SIGTERM shutdown adapter for a clean foreground stop. Added Plan 0098 and DEVELOPMENT.md guidance.
- Incidental fix: discovered `http.server.HTTPServer.server_bind` calls `socket.getfqdn(host)`, which stalled ~35s per server start in this environment's DNS-less sandbox. Added `_LoopbackHTTPServer` overriding `server_bind` to skip that reverse lookup (the host is always an already-validated loopback literal), cutting the focused suite from 36s to under 1s.
- Tests: new focused suite `tests/test_api.py`, `28 passed` — loopback-host validation, routing/JSON-contract parity against both the shared payload functions and live CLI (`run list`/`run inspect`/`run history`) output, structured 404 (unknown run, unrecognized path) and 405 (unsupported method) with non-mutation assertions, query-string tolerance, read-only state opening, and CLI-level clean shutdown on SIGINT/SIGTERM. Full suite `700 passed` (up from 672, +28 net) — no regressions from the payload extraction.
- Verification: activated `.venv`; rebuilt and checked the index (27 files, 1050 symbols, 6180 relationships); `git diff --cached --check` clean; live UAT via direct `main()` invocation (subprocess-based UAT was not possible — this sandbox blocks child-process network I/O even on loopback, confirmed independently of this feature) confirmed `api serve` prints its bound host/port, serves real `run list`/`run inspect` JSON over loopback HTTP against a live state database, and shuts down cleanly on SIGINT.
- Durable state: implementation commit `7927101` pushed to `origin/main`; #105 closed with commit and verification evidence.
- Blocked review: #106's sole stated blocker (#105) is now closed, so `blocked` was removed and `agent-ready` added with an evidence comment. #107 (blocked by #105 *and* #106) and #108 (blocked by #105, #106, #107) remain correctly `blocked` since #106/#107 are still open; no comment added since their blocker state did not materially change. Sprint 17 now has one ready issue: #106 (priority:2).
- Roadmap horizon: 12 ordered open milestones before and after this run (Sprint 17 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was created.
- Next eligible action: implement #106 (expose approvals and usage over the read-only HTTP API, reusing `_approval_payload`/`_usage_payload`) in Sprint 17. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.

---

- Run: 2026-07-14T11:34:35Z — retrospective run (scheduled).
- Active milestone at start: Sprint 16 "Live run observation" (#16). All three implementation issues (#101-#103) were closed, so no implementation issue was selected; created retrospective issue #104.
- Completed: exercised the public `run watch` workflow end to end. Live polling emitted queued/running/succeeded history exactly once in sequences 1-3 and stopped at terminal state; restart with `--after-sequence 1` emitted only sequences 2-3; a pending approval emitted one stable `approval_pending` blocked event; sensitive command arguments, environment names/values, provider request bodies, and terminal output remained absent; a missing database failed cleanly and remained absent. All four milestone exit criteria passed. Decisions 0005/0006 remain upheld because watch composes existing durable read models without adding a mutation path, persistence, or transport. #104 records the evidence and is closed; Sprint 16 is closed with no remediation milestone.
- Verification: activated `.venv`; focused watch suite `12 passed, 228 deselected in 0.45s`; full suite `672 passed in 8.38s`; `codex-agentic-os index check` current; `git diff --check` clean; live CLI UAT covered ordered polling, terminal exit, cursor resume, clean SIGINT, approval-blocked redaction, and missing-state non-creation.
- Durable state: product evidence remains commits `0bce819` (#101), `bd5b276` (#102), and `2edcbb9` (#103), all on `origin/main`; retrospective issue #104 and milestone #16 were closed in GitHub. This run changes only this durable MEMORY record and commits/pushes it on `main`.
- Blocked review: repository-wide open `blocked` search is empty. Resulting active Sprint 17 has 0 open and 0 ready issues.
- Roadmap horizon: 13 ordered open milestones before retrospective closure (Sprint 16 through Sprint 28), 12 after (Sprint 17 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was created.
- Next eligible action: replenishment-only review for Sprint 17 "Read-only operator HTTP API"; create only issues that close its stated exit-criteria gaps, without implementing one in the same run. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.

---

- Run: 2026-07-14T11:02:47Z — implementation run (scheduled).
- Active milestone: Sprint 16 "Live run observation" (#16). Selected #103 (priority:3), the sole open/`agent-ready` issue; its stated dependencies #101/#102 were closed.
- Completed: finalized the `run watch` redaction/approval-blocked output contract. No runtime or CLI behavior changed — `RunHistoryEntry` (Plans 0060/0062) never stored raw command arguments, resolved environment values, provider request bodies, or terminal output, and `_watch_blocked_step` (Plan 0095) only ever surfaces `step_id`/`position`/`objective`/`reason`, so the redaction guarantee was already structural. Added the regression the milestone's acceptance criteria required: a focused test drives a command step with sensitive command arguments and `SandboxPolicy.env_passthrough`, a completed step's captured terminal output, and a pending-approval provider step with a sensitive message body through `run watch`, then asserts none of those values appear anywhere in watch output and that every emitted `history`/`blocked` entry's keys stay within the documented allowlist. Added Plan 0097.
- Tests: focused watch suite `12 passed` (up from 11, +1 net); full suite `672 passed` (up from 671, +1 net).
- Verification: activated `.venv`; rebuilt and checked the index (24 files, 1012 symbols, 5966 relationships); `git diff --check` clean; live CLI UAT created a run with a pending-approval command step, watched it, confirmed the single redacted `blocked` notice with no command/env data, and interrupted the watcher cleanly with SIGINT.
- Durable state: implementation commit `2edcbb9` pushed to `origin/main`; #103 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search is empty. Sprint 16 now has 0 open issues (#101-#103 all closed) — ready for a retrospective run.
- Roadmap horizon: 13 ordered open milestones before and after this run (Sprint 16 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was created.
- Next eligible action: run Sprint 16's retrospective (all issues closed, exit criteria ready for end-to-end review) — do not select a new implementation issue until the retrospective closes or remediates the milestone. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.

---

- Run: 2026-07-14T10:33:01Z — implementation run (scheduled).
- Active milestone: Sprint 16 "Live run observation" (#16). Selected #102 (priority:2), the sole unblocked `agent-ready` issue; its stated dependency #101 was closed.
- Completed: added `run watch --after-sequence N`, a non-negative operator-provided durable history cursor. The existing read-only polling loop initializes its in-process sequence from N and emits only history entries with sequence greater than N, so a watcher stopped after entry N can restart without duplicates or gaps. Validation occurs before the state database is opened; no automatic watcher state, persistence, subscription, or transport was added. Durable sequence was already present in each machine-readable history line. Added Plan 0096 and DEVELOPMENT.md guidance.
- Tests: focused watch suite `11 passed`; full suite `671 passed` (up from 668, +3 net), covering negative cursor rejection without database creation, stop/restart after sequence 1 with later sequences 2–3 emitted exactly once in order, terminal exit, and read-only preservation of run, step, and history state.
- Verification: activated `.venv`; rebuilt and checked the index (24 files, 1010 symbols, 5947 relationships); `git diff --check` clean; live CLI UAT created and cancelled a temporary run, then resumed after sequence 1 and emitted only terminal sequence 2.
- Durable state: implementation commit `bd5b276` pushed to `origin/main`; #102 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search is empty. #103's dependencies #101/#102 are both closed, so `blocked` was removed and `agent-ready` added with an evidence comment. Sprint 16 now has one open/ready issue: #103 (priority:3).
- Roadmap horizon: 13 ordered open milestones before and after this run (Sprint 16 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was created.
- Next eligible action: implement #103 (finalize and regression-test the stable redacted history and approval-blocked `run watch` output contract) in Sprint 16. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.
