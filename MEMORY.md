# Automation Memory

- Run: 2026-07-14T02:13:00Z — implementation run.
- Active milestone at start: Sprint 13 "Operator-accepted model plan decomposition" (#13), holding 4 issues created outside a recorded automation run (#87 `agent-ready`/priority:1; #88, #89, #90 `blocked` on #87). Selected #87, the sole `agent-ready` issue.
- Completed: added `RunCoordinator.propose_plan` (`src/codex_agentic_os/runtime.py`) and `codex-agentic-os run plan <run_id> <plan_id> --provider ... [--model] [--objective] [--temperature] [--max-tokens]` (`src/codex_agentic_os/cli.py`). It dispatches the run's objective (or an explicit `--objective` override) plus a fixed planning system prompt through the existing `ChatAdapterResolver`/`_provider_adapter_resolver()` boundary (same path `run add-step`/`run execute-next` already use), validates the minimal accepted proposal shape (`{"steps": [{"objective": str, "execution_kind": "command"|"provider"}, ...]}`), and persists a durable draft. Reused the `plan` `StateStore` kind — already present in `state.py`'s schema but unused anywhere in the codebase until now, so no migration was needed. Success persists a `draft` plan record (ordered steps plus provider evidence: provider, requested/response model, raw content, raw response body). A malformed/unparseable proposal instead persists an `invalid` plan record carrying the same raw evidence and raises `PlanProposalError` (a `ValueError`) naming the recorded plan id; no steps are ever queued by this command, success or failure. Missing run, terminal run, and duplicate plan id are all rejected before any adapter dispatch (verified via an `AssertionError`-raising fake resolver in tests). Added Plan 0084 and a `run plan` DEVELOPMENT.md section next to the other run-step commands.
- Verification: activated `.venv`; new `tests/test_runtime.py::test_propose_plan_*` (13 tests covering successful persistence/evidence, objective default vs. override, 8 malformed-shape cases each asserting raw evidence preservation and zero queued steps, duplicate-plan-id rejection without a second dispatch, missing-run, and terminal-run) and new `tests/test_run_cli.py::test_cli_plan_*` (4 tests: end-to-end draft creation against an injected fake adapter via `codex_agentic_os.cli.adapter_for` monkeypatching — the same pattern `run execute-next`'s provider-message tests already use — objective default/override, malformed-proposal CLI failure with exit code 2 and preserved evidence, missing-run rejection before adapter resolution) all pass. Full suite `548 passed` (up from 531); committed index rebuilt/current (23 files, 813 symbols, 4738 relationships); `git diff --check` clean.
- Durable state: commit `a3d7bbe` pushed to `origin/main`; issue #87 closed with a verification comment (commit trailer alone would not auto-close since the message references `(#87)` without a `Closes`/`Fixes` keyword, so closure was explicit via `gh issue close`).
- Blocked review: repository-wide `blocked`-label search found #88, #89 (both blocked only on #87) and #90 (blocked on #87, #88, #89). #87's closure resolved #88's and #89's sole dependency, so `blocked` was removed and `agent-ready` added to both with an evidence comment; both are now `priority:2` and tied, so the next eligible run breaks the tie by oldest creation time. #90 remains correctly `blocked` (still depends on open #88/#89).
- Roadmap horizon: 16 open milestones before and after this run (Sprint 13 through Sprint 28), above the three-sprint threshold, so no planning handoff was needed.
- Final target: `main`; worktree dirty only for this MEMORY update before commit.

---

- Run: 2026-07-14T01:00:00Z — retrospective run with milestone closure.
- Active milestone at start: Sprint 12 "Autonomous worker loop" (#12), holding 0 open issues (#82, #83, #84, #85 all closed), so this run was a retrospective against its exit criteria — no issue was implemented. Sprint 13 "Operator-accepted model plan decomposition" (#13) is now active with 0 issues.
- Completed: exercised the full Sprint 12 vertical slice from an operator's perspective on a fresh scratch database with real live Docker dispatch (no mocks), not issue closure alone. Registered agent `uat-agent`; created `run-a` (one agent-assigned command step), `run-b` (one `--approval-required` command step), `run-c` (one command step added later). Launched a real foreground `worker run --agent-id uat-agent --heartbeat-interval 5 --poll-interval 3` process and observed it end-to-end: `run-a`'s step dispatched through a real `docker run --rm --network none --read-only --cpus 2 --memory 4g python:3.12-slim …` invocation and completed (`run inspect` from a separate process showed `succeeded`, `exit_code: 0`, real stdout durably recorded); `run-b`'s approval-gated step stayed untouched (`queued`, no output) through a full idle poll cycle, then was claimed and completed automatically on the very next poll cycle after an out-of-band `run approve step-b1` with no worker re-invocation; every dispatched step's recorded `output.command` was built solely from its persisted `sandbox_policy` (worker CLI exposes no sandbox override flags, confirmed via `--help`); a real `SIGINT` sent to the live worker process produced a clean JSON summary (`claimed_run_ids: [run-a, run-b, run-c]`, `executed_step_ids: [step-1, step-b1, step-c1]`) on stdout, empty stderr, no traceback, no orphaned `running` step. All 5 milestone exit criteria recorded as passing with this evidence plus the pre-existing 531-test full suite (mid-execute-call SIGINT/SIGKILL-recovery paths are covered there via a real signal delivered during a patched `ContainerSandbox.execute`, and a leftover `running` step from a simulated hard kill rejected via existing `run recover`/`run reassign-claim`). Noted (not a defect, no code change): step ids are unique across the whole state DB, not scoped per run.
- Verification: activated `.venv`; full suite `531 passed` (unchanged, no code modified this run); `codex-agentic-os index check` reports current (23 files, 780 symbols, 4600 relationships); `git diff --check` clean; scratch DB and worker logs removed after use, no repository files touched.
- Durable state: retrospective issue #86 created in milestone #12 with full criterion-by-criterion evidence, closed with a verification comment; milestone #12 closed via `gh api … -X PATCH -f state=closed`. No implementation commit this run (retrospective-only); this MEMORY update is the only pending change.
- Blocked review: repository-wide `blocked`-label search found 0 open issues.
- Roadmap horizon: 17 open milestones before this run's closure (Sprint 12 through Sprint 28) and 16 after (Sprint 13 through Sprint 28), still above the three-sprint threshold, so no planning handoff occurred and no milestones were created.
- Final target: `main`; worktree dirty only for this MEMORY update before commit. Next eligible run: Sprint 13 "Operator-accepted model plan decomposition" (#13) has 0 issues, so the next run is replenishment against its exit criteria, not implementation.

---

- Run: 2026-07-14T00:00:00Z — implementation run.
- Active milestone: Sprint 12 "Autonomous worker loop" (#12), holding 1 open issue (#85, priority:3, the sole `agent-ready` issue) at start.
- Completed: `worker run`'s `cli.py` dispatch now installs SIGINT/SIGTERM handlers (`_install_worker_shutdown_signals`) that flip the same kind of `should_continue` flag `run_worker` already supported for deterministic tests (added in Plan 0080), instead of letting Python's default disposition raise `KeyboardInterrupt` past `main`'s blanket `except (ValueError, RuntimeError)` handler (producing a raw traceback) or SIGTERM's default disposition kill the process outright with no summary. `worker.py` needed no change: its outer/inner loops already only check `should_continue` between steps, never mid-dispatch, so a step already executing when a signal arrives always finishes and is recorded durably before the loop stops on the next check; the prior signal disposition is restored in a `finally` block regardless of outcome. The "genuinely in-flight uncertain step" acceptance criterion required no new runtime behavior either — `execute_next_step`'s pre-existing "run already has a running step" `ValueError` guard already rejects dispatch onto a step left `running` by a hard kill the worker can't catch (e.g. `SIGKILL`), so it's never silently completed or duplicated, and it remains reconcilable via the pre-existing `run recover` / `run reassign-claim` commands (Sprints 8-9). Updated the `worker run` DEVELOPMENT.md section to drop the "does not yet handle interruption cleanly" caveat and describe the shutdown/recovery contract. Added Plan 0083.
- Verification: activated `.venv`; new `tests/test_worker_cli.py` tests — real SIGINT and SIGTERM delivered to the running test process during `worker run` (via `os.kill(os.getpid(), ...)` inside a patched `ContainerSandbox.execute`) exit cleanly with the normal JSON summary and no exception, restoring the prior signal disposition; a leftover `running` step from a simulated hard kill (`coordinator.start_next_step` called directly, bypassing `execute_next_step`) is rejected via `SystemExit(2)` — not duplicated or silently completed — on a later `worker run` invocation, then successfully reconciled via `run recover ... interrupted`; an end-to-end mixed command/provider run delegated entirely to `worker run` (no manual `run execute-next`) is reconstructed from fresh `run inspect`/`run history` calls, confirming both steps and the run succeeded. Repeated the two signal tests 5x locally (10 signal deliveries) with no flakiness. Full suite `531 passed` (up from 527); committed index rebuilt/current (23 files, 780 symbols, 4600 relationships); `git diff --check` clean.
- Durable state: commit `35d49f3` pushed to `origin/main`; issue #85 auto-closed via its commit trailer and received a verification comment.
- Blocked review: repository-wide `blocked`-label search found 0 open issues. Sprint 12 now has 0 open issues (#82, #83, #84, #85 all closed), so the next eligible run is a retrospective against Sprint 12's exit criteria — no issue should be implemented during that run.
- Roadmap horizon: 17 open milestones before and after this run (Sprint 12 through Sprint 28), above the three-sprint threshold, so no planning handoff was needed.
- Final target: `main`; worktree dirty only for this MEMORY update before commit.

---

- Run: 2026-07-13T23:07:04Z — implementation run.
- Active milestone: Sprint 12 "Autonomous worker loop" (#12), holding 2 open issues (#84, #85) at start. #84 was the sole `agent-ready` issue (priority:2); #85 remained `blocked`.
- Completed: no `src/codex_agentic_os/` source changed. Issue #84's own triage comment established that #82's `_persisted_sandbox_resolver` extraction already routed worker command dispatch exclusively through a step's persisted `sandbox_policy` (never an ad hoc `executor`), that `worker run`'s argparse parser already exposed no sandbox override flags, and that `execute_next_step` already failed a policy-less command step through its existing explicit `ValueError` path. Read-verified all of this directly in `cli.py`/`runtime.py`/`worker.py`, confirming every acceptance criterion already held. This run's scope was therefore entirely regression-test coverage locking in that contract: added `tests/test_worker.py::test_run_worker_command_step_without_persisted_policy_fails_without_ad_hoc_executor` (proves the injected `sandbox_resolver` is never called and the step stays `QUEUED` when no policy is persisted); added to `tests/test_worker_cli.py` a `--help` flag audit, a real-CLI-path missing-policy failure test, a real-CLI-path missing-`env_passthrough`-variable failure test, and a real-CLI-path test proving a present `env_passthrough` variable is resolved by name only via a patched `ContainerSandbox.execute` and never leaks into durable `run inspect` output. Added Plan 0082.
- Verification: activated `.venv`; new/extended `tests/test_worker.py` and `tests/test_worker_cli.py` suites pass (28/28 combined); full suite `527 passed` (up from 522); committed index rebuilt/current (23 files, 769 symbols, 4540 relationships); `git diff --check` clean.
- Durable state: commit `43250e0` pushed to `origin/main`; issue #84 closed with a verification comment (no auto-close trailer used since no source changed, so the issue was closed explicitly via `gh issue close`).
- Blocked review: repository-wide `blocked`-label search found only #85, blocked on #82/#83/#84. All three are now closed, so `blocked` was removed and `agent-ready` added to #85 with an evidence comment. Sprint 12 now has one ready issue: #85 (priority:3), the next eligible implementation.
- Roadmap horizon: 17 open milestones before and after this run (Sprint 12 through Sprint 28), above the three-sprint threshold, so no planning handoff was needed.
- Final target: `main`; worktree dirty only for this MEMORY update before commit.

---

- Run: 2026-07-13T22:07:00Z — implementation run.
- Active milestone: Sprint 12 "Autonomous worker loop" (#12), holding 3 open issues (#83, #84, #85) at start. Prior MEMORY recorded #83/#84 tied on `priority:2` and broke the tie by oldest creation time in favor of #84 (recorded as created 20:17:14Z vs #83's 20:17:15Z) — but live GitHub data at run start showed the opposite: #83 created `2026-07-13T20:17:14Z`, #84 created `2026-07-13T20:17:15Z`. Trusted the live evidence per the skill's "GitHub milestone IDs/records are current state" guidance and selected #83 (make worker skip ineligible work and idle deterministically) as the older of the tied-priority pair.
- Completed: `run_worker` (`src/codex_agentic_os/worker.py`) now catches `ApprovalRequiredError`/`ContextReferencesUnresolvedError` raised by `RunCoordinator.execute_next_step` instead of letting them propagate uncaught — both are raised by `start_next_step` strictly before any durable mutation, so catching them requires no rollback. A per-invocation `blocked_run_ids` set accumulates run ids that failed to dispatch this poll cycle; `_claim_eligible_run` excludes them so the worker immediately tries another assigned or claimable run instead of retrying the same blocked step. Broadened `_claim_eligible_run`'s "already assigned to this agent" match from `RunStatus.QUEUED`-only to any non-terminal status (`QUEUED` or `RUNNING`), because a run blocked after one or more steps already succeeded is `RUNNING`, not `QUEUED`, and `claim_next` never reclaims an already-claimed `RUNNING` run — without the broadened match such a run would become permanently invisible to the worker after its first block. When no unblocked run is selectable, the worker sleeps exactly one `poll-interval` via the injected sleeper and clears `blocked_run_ids`, so a step that becomes eligible out of band (an operator `run approve`, or an earlier context step finishing) is retried automatically on the next cycle rather than being abandoned or busy-spun. Updated the `worker run` DEVELOPMENT.md section to describe this and removed the old "does not yet skip" caveat. Added Plan 0081.
- Verification: activated `.venv`; `tests/test_worker.py` extended with 4 new tests (approval-blocked run skipped in favor of another eligible run; unresolved-context-reference run skipped without mutation via a directly `cancel_step`-cancelled context dependency; deterministic idle — exactly 3 sleeper calls of the configured `poll_interval` across a 9-iteration bounded cycle, worked out by hand since each blocked-then-idle cycle consumes 3 `should_continue` iterations (claim blocked run, attempt+fail dispatch, find-none-and-sleep) — with no exception; a blocked run resumed and completed on a later `run_worker` invocation after an out-of-band `coordinator.approve_step`), 16/16 pass; full suite `522 passed` (up from 518); committed index rebuilt/current (23 files, 761 symbols, 4490 relationships); `git diff --check` clean. Skipped a live-docker CLI UAT since `worker run`'s command dispatch requires a persisted sandbox policy and this environment's docker/podman availability is unverified; the library-level tests exercise the real `RunCoordinator`/`AgentRegistry`/SQLite `StateStore` stack (not mocked), which is the appropriate verification depth for this issue — an end-to-end delegate-and-walk-away operator review is explicitly issue #85's scope, not #83's.
- Durable state: implementation commit `1b36e09` pushed to `origin/main`; issue #83 auto-closed via its commit trailer and received a verification comment.
- Blocked review: repository-wide `blocked`-label search found only #85 (blocked on #82, #83, #84). #82 and #83 are now both closed but #84 remains open, so #85 correctly stays `blocked`; no label changes made. Sprint 12 now has one ready issue: #84 (priority:2), the next eligible implementation.
- Roadmap horizon: 17 open milestones before and after this run (Sprint 12 through Sprint 28), above the three-sprint threshold, so no planning handoff was needed.
- Final target: `main`; worktree dirty only for this MEMORY update before commit.
