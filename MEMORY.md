# Automation Memory

- Run: 2026-07-13T16:34:09Z — implementation run.
- Active milestone: Sprint 10 "Provider usage and cost evidence" (#10). Selected the sole unblocked `agent-ready` issue, #75 (persist provider usage evidence in durable step output, priority:2). Sprint 10 still has open issue #76, so its exit criteria are not yet ready for the retrospective/close-or-remediate mode.
- Completed: every successful provider-message step now durably persists a stable five-field `usage` object (`available`, `input_tokens`, `output_tokens`, `raw`, `unavailable_reason`) alongside the existing `content`, `model`, and provider-response `raw` fields. Available counts plus the provider raw usage block and explicit unavailable evidence both survive process restart; provider completion and adapter-failure behavior are unchanged. Added Plan 0075; no README/DEVELOPMENT change because the dedicated operator `run usage` workflow remains #76.
- Verification: activated `.venv`; focused runtime/CLI suites passed (`323 passed` across the affected files, including 4 directly usage-focused tests); full suite `472 passed`; a three-process SQLite UAT created/reran/inspected the durable usage evidence with no request or system-prompt leakage into step output; committed index rebuilt/current (20 files, 664 symbols, 3950 relationships); `git diff --check` clean.
- Durable state: implementation commit `42459a6` pushed to `origin/main`; issue #75 closed with commit and verification evidence. This MEMORY entry's durable commit/push is pending.
- Blocked review: repository-wide review found #76's sole dependency #75 resolved; removed `blocked`, added `agent-ready`, and recorded the evidence. No issue now carries a `blocked` label. Sprint 10 has one ready issue: #76.
- Roadmap horizon: 19 open milestones before and after this run (Sprint 10 through Sprint 28), above the three-sprint threshold, so no planning handoff occurred and no milestones changed.
- Final target: `main`; worktree dirty only for this MEMORY update before commit. Next eligible issue: #76.

---

- Run: 2026-07-13T16:05:38Z — implementation run.
- Active milestone: Sprint 10 "Provider usage and cost evidence" (#10). At run start it already held three issues (#74 agent-ready priority:1, #75 blocked priority:2, #76 blocked priority:3) from a prior, unrecorded replenishment pass since the last durable MEMORY entry; this entry restores the record. Selected the sole unblocked `agent-ready` issue, #74 (normalize provider usage evidence from chat adapters).
- Completed: added a frozen `ChatUsage` value (`available`, `input_tokens`, `output_tokens`, `raw`, `unavailable_reason`) to `chat.py`, attached to `ChatResponse.usage` with an explicit-unavailable default so every response always carries usage evidence. `OpenAICompatibleAdapter` extracts `usage.prompt_tokens`/`completion_tokens`, `AnthropicAdapter` extracts `usage.input_tokens`/`output_tokens`, `GoogleAdapter` extracts `usageMetadata.promptTokenCount`/`candidatesTokenCount`; each degrades explicitly to unavailable (with a reason) when the block is missing or the counts are malformed, without changing existing content/model/raw behavior. Exported `ChatUsage` from the package root. Persistence into durable step output (#75) and the `run usage` CLI (#76) are explicitly out of scope, reserved for those issues. Added Plan 0074; no README/DEVELOPMENT change since this normalization is not yet operator-visible.
- Verification: activated `.venv`; focused `tests/test_chat.py` suite passed (`25 passed`, 10 new usage tests covering all three adapter families and their unavailable paths); full suite `471 passed` (up from 465); committed index rebuilt/current (20 files, 661 symbols, 3933 relationships); `git diff --check` clean.
- Durable state: implementation commit `75d8bb4` pushed to `origin/main`; issue #74 auto-closed via `Closes #74` trailer and received a verification comment. This MEMORY entry's durable commit/push is pending.
- Blocked review: repo-wide `blocked` search found #75 and #76. #75's sole dependency #74 is now closed, so `blocked` was removed and `agent-ready` added with an evidence comment. #76 remains correctly blocked on open #75. Sprint 10 has one ready issue: #75.
- Roadmap horizon: 19 open milestones before and after this run (Sprint 10 through Sprint 28), above the three-sprint threshold, so no planning handoff occurred and no milestones changed.
- Final target: `main`; worktree dirty only for this MEMORY update before commit. Next eligible issue: #75.

---

- Run: 2026-07-13T15:40:02Z — implementation run with post-implementation retrospective and milestone closure.
- Active milestone at start: Sprint 9 "Persisted per-step sandbox policy" (#9). Selected its sole unblocked `agent-ready` issue, #72 (execute command steps from persisted sandbox policy, priority:2); implementation issues #71/#72 and retrospective issue #73 are now closed, and milestone #9 is closed without remediation. Sprint 10 "Provider usage and cost evidence" (#10) is active with 0 issues.
- Completed: persisted-policy command steps now dispatch only through a policy-aware runtime resolver; an injected executor cannot bypass the durable policy. `run execute-next` constructs the full sandbox from the persisted kind/image/mounts/workdir/network decision, resolves passthrough names from the executing process on every dispatch, and rejects all per-invocation sandbox flags before mutation. Missing worker variables also fail before mutation. The recorded engine command redacts resolved `--env NAME=value` arguments to names only, closing a leak exposed by live Docker review. Legacy unpersisted command steps retain `--sandbox ...`; provider-message dispatch is unchanged. Added Plan 0073 and DEVELOPMENT guidance.
- Verification: activated `.venv`; focused runtime/CLI persisted-policy suites passed (`19 passed`); full suite `465 passed`; committed index rebuilt/current (20 files, 644 symbols, 3879 relationships); `git diff --check` clean. Fresh-process Docker UAT dispatched with no sandbox flags, proved a worker-only value reached the container while only its name was persisted, proved the raw value absent from SQLite, and rejected conflicting `--network` with the run and step still queued. Retrospective #73 recorded all five Sprint 9 exit criteria passing with no known defect or remediation need.
- Durable state: implementation commit `134b4a5` pushed to `origin/main`; issue #72 auto-closed and received verification evidence; retrospective #73 created, completed, and closed; milestone #9 closed. This MEMORY entry's durable commit/push is pending.
- Blocked review: no open `blocked` issues exist repository-wide. Sprint 10 has 0 ready issues, so the next eligible run is replenishment against its exit criteria; do not implement during that run.
- Roadmap horizon: 20 open milestones before Sprint 9 closure and 19 after (Sprint 10 through Sprint 28), above the three-sprint threshold, so no planning handoff occurred and no milestones were added.
- Final target: `main`; worktree dirty only for this MEMORY update before commit.

---

- Run: 2026-07-13T15:12:00Z — implementation run.
- Active milestone: Sprint 9 "Persisted per-step sandbox policy" (#9). At run start it already held two issues (#71 agent-ready priority:1, #72 blocked priority:2) that a prior, unrecorded replenishment pass had created between the last durable MEMORY entry (06:37:25Z) and this run; this entry restores the record. Selected the sole unblocked `agent-ready` issue, #71 (persist per-step sandbox policy at command step creation).
- Completed: added a durable `SandboxPolicy` view (`kind`, `image`, `mounts`, `working_dir`, `env_passthrough` names, `network_enabled`) reusing `SandboxKind` from `sandboxes.py`. `RunStep.sandbox_policy` and `RunCoordinator.add_step(..., sandbox_policy=...)` validate independently of the CLI (invalid kind, empty image, malformed mount, relative/empty workdir, malformed/duplicate env-passthrough name, non-bool network all reject before any queued step is appended). The policy is preserved across every existing step payload rewrite (`cancel`, `start_next_step`, `transition_step`, `_decision_payload`, `complete_step_from_result`, `fail_step_from_error`, `recover_running_step`) and through `StateStore.retry_failed_step`, so it survives the full step lifecycle, not just initial creation. `run add-step` gained `--sandbox`, `--image`, `--mount`, `--env-passthrough`, `--workdir`, `--network`; any one requires `--sandbox` and is rejected for provider-message steps. `run inspect`/`inspect-step` show the policy (omitted when absent, matching the existing `message` convention) with nothing to redact since only variable names are ever persisted. Added Plan 0072 and DEVELOPMENT usage guidance. Dispatch-time consumption (`run execute-next` resolving passthrough values and rejecting conflicting per-invocation flags) is explicitly out of scope, reserved for #72.
- Verification: activated `.venv`; focused runtime/CLI suites passed; full suite `452 passed` (up from 433); committed index rebuilt/current (20 files, 629 symbols, 3799 relationships); `git diff --check` clean. Fresh-process CLI UAT persisted a full policy, reloaded it identically from a separate process via `run inspect-step`, confirmed the raw SQLite payload holds only env-passthrough variable names (never resolved values), and rejected a malformed policy (`--workdir relative/path`) before any queued step was appended.
- Durable state: implementation commit `fb581bb` pushed to `origin/main`; issue #71 auto-closed via `Closes #71` trailer and received a verification comment. This MEMORY entry's durable commit/push is pending.
- Blocked review: repo-wide `blocked` search found only #72, whose sole dependency #71 is now closed; removed `blocked` and added `agent-ready` with an evidence comment. No other open `blocked` issues exist. Sprint 9 has one ready issue: #72.
- Roadmap horizon: 20 open milestones before and after this run (Sprint 9 through Sprint 28), above the three-sprint threshold, so no planning handoff occurred and no milestones changed.
- Final target: `main`; worktree dirty only for this MEMORY update before commit. Next eligible issue: #72.

---

- Run: 2026-07-13T06:37:25Z — implementation run with post-implementation retrospective and milestone closure.
- Active milestone at start: Sprint 8 "Explicit failed-step retry" (#8). Selected its sole unblocked `agent-ready` issue, #69 (retry a failed step from the CLI, priority:3); implementation issue #69 and retrospective issue #70 are now closed, and milestone #8 is closed without remediation. Sprint 9 "Persisted per-step sandbox policy" (#9) is active with 0 issues.
- Completed: added explicit `run retry-step STEP_ID NEW_STEP_ID --expected-step-revision --expected-run-revision`, delegating only to the atomic CAS runtime path. `run inspect`/`inspect-step` derive bidirectional `retried_into_step_id` / `retried_from_step_id` lineage from durable `step_retried` history. Command/provider terminal completion now excludes only failed attempts proven superseded by that history, so the successful retry can finish the run; approval-required retries still reset to pending and use the existing approval path. Added Plan 0071 and DEVELOPMENT guidance.
- Verification: activated `.venv`; focused runtime/CLI retry suites passed; full suite `433 passed`; committed index rebuilt/current (20 files, 617 symbols, 3694 relationships); `git diff --check` clean. Fresh-process Docker UAT failed an initial marker-check command, created the retry with explicit inspected revisions, changed only external marker state, executed the identical persisted command successfully, and reconstructed failed→retry→succeeded lineage plus ordered history from SQLite alone. The retrospective recorded all four Sprint 8 exit criteria passing with no known defect or remediation need.
- Durable state: implementation commit `4878a4c` pushed to `origin/main`; issue #69 auto-closed and received verification evidence; retrospective #70 created, completed, and closed; milestone #8 closed. This MEMORY entry's durable commit/push is pending.
- Blocked review: no open `blocked` issues exist repository-wide. Sprint 9 has 0 ready issues, so the next eligible run is replenishment against its exit criteria; do not implement during that run.
- Roadmap horizon: 21 open milestones before Sprint 8 closure and 20 after (Sprint 9 through Sprint 28), above the three-sprint threshold, so no planning handoff occurred and no milestones were added.
- Final target: `main`; worktree dirty only for this MEMORY update before commit.
