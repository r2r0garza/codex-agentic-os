# Automation Memory

- Run: 2026-07-14T20:38:00Z — implementation run (scheduled).
- Active milestone at start: Sprint 20 "Parent-child run delegation" (#20). Selected #122 (priority:1, `agent-ready`), the sole unblocked issue; its dependency #121 was closed. #123/#124 remained blocked.
- Completed: `RunCoordinator.execute_next_step` now reconciles a running delegation step against its linked child run. Queued/running children raise `DelegationPendingError` without mutation. A succeeded child atomically succeeds the parent step, advances to later queued work when present, or succeeds the final parent run; a failed or cancelled child fails both the parent step and run explicitly. Parent-step output durably records `child_run_id`, terminal `child_status`, the child's terminal output, and `child_agent_id` when known. Parent history uses `execution_kind="delegation"`; both final step+run transitions and non-final successful-step transitions are revision-checked, with conflicts surfaced without overwrite. CLI `run execute-next` now re-enters a running delegation for pending/terminal reconciliation instead of returning early because no queued step exists. Worker coverage proves a target agent can finish the child and the parent agent then completes the delegation. Updated DEVELOPMENT.md and added Plan 0111.
- Verification: activated `.venv`; focused runtime delegation tests 16 passed, worker delegation tests 2 passed, CLI delegation test 1 passed; full `pytest` 786 passed (up from 778); `codex-agentic-os index build` refreshed 27 files / 1137 symbols / 6953 relationships; `index check` current; `git diff --check` clean.
- Durable state: implementation commit `fcc4ff7` pushed to `origin/main`; #122 closed with commit and verification evidence.
- Blocked review: repository-wide search found #123 and #124. #123 depended only on now-closed #121/#122, so removed `blocked`, added `agent-ready`, and commented with the available atomic spawn/reconciliation contracts. #124 remains correctly blocked on open #123.
- Retrospective state: Sprint 20 has two open issues (#123/#124), so close-or-remediate review is not yet eligible.
- Roadmap horizon: 9 ordered open milestones before and after this run (Sprint 20 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was needed.
- Next eligible action: implement #123 (recover delegated child spawn/linkage without orphans or duplicates across interruption). Final target `main`; after this MEMORY handoff commit, the worktree is clean except for the preserved unrelated untracked `.claude/` directory.

---

- Run: 2026-07-14T20:41:00Z — implementation run (scheduled).
- Active milestone at start: Sprint 20 "Parent-child run delegation" (#20). GitHub showed issues #121-#124 already created (replenishment pass at 2026-07-14T19:47:29-33Z whose `MEMORY.md` record was never committed, mirroring prior gaps). Treated GitHub as authoritative and selected #121 (priority:1, `agent-ready`, no stated dependency), the sole unblocked issue; #122/#123/#124 remained correctly `blocked`.
- Completed: added a durable child-run delegation step. `DelegationSpec` (child objective + optional target agent) is a third mutually-exclusive step execution input alongside command/provider message, validated at `add_step` declaration time. `StateStore.dispatch_delegation_step` performs one `BEGIN IMMEDIATE` atomic transaction that CAS-checks the queued step, inserts the new linked child run (deterministic id `{step_id}-child`), transitions the parent step to `running` with `delegated_run_id` recorded, and transitions the parent run `queued -> running` only when it was still queued — a competing/repeated dispatch conflicts on the step's own revision rather than racing to create two children. `RunCoordinator.execute_next_step` dispatches a queued delegation step through this path; a second dispatch attempt on the now-running step raises `DelegationPendingError` instead of the generic "already running" conflict, and `start_next_step` now rejects delegation steps outright since its two-phase dispatch/complete model doesn't fit atomic spawn+link. `worker.py`'s poll loop treats `DelegationPendingError` like the existing approval/context-reference blocked-run signals so a parked delegation step never crashes it. Added `AgentRun.parent_run_id`/`parent_step_id` and a shared `_base_run_payload` helper used across every existing run-payload rewrite site (`transition`, `cancel`, `start_next_step`, `complete_step_from_result`, `complete_step_from_chat_response`, `fail_step_from_error`, `recover_running_step`, `reject_step`) so parent linkage (and `agent_id`) survive a child run's own ordinary lifecycle transitions instead of being silently dropped on first write. CLI gained `run add-step --delegate-objective/--delegate-target-agent` and an `execute-next` branch for delegation steps. DEVELOPMENT.md documents the new declaration/dispatch contract; added Plan 0110.
- Verification: activated `.venv`; full `pytest` 778 passed (up from 760; +18 new/changed cases across `test_runtime.py` (12), `test_state.py` (3), `test_worker.py` (1), `test_run_cli.py` (2), plus 5 pre-existing CLI tests updated for the new `parent_run_id`/`parent_step_id` payload fields and the updated three-way mutual-exclusivity error text); `codex-agentic-os index build` (source changed) then `index check` current at 27 files, 1129 symbols, 6841 relationships; `git diff --check` clean. Manual CLI smoke test confirmed atomic child spawn/link, correct parent/child linkage via `run inspect`, and a graceful `attempted: false` (no duplicate child) on a second dispatch attempt.
- Durable state: implementation commit `4f18d57` pushed to `origin/main`; #121 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search found #122 (blocked solely on #121, now closed), #123 (blocked on #121 and #122), and #124 (blocked on #121, #122, #123). Removed `blocked` and added `agent-ready` to #122 with an evidence comment describing the delegation/linkage contract it builds on. #123 and #124 remain correctly `blocked` on #122 (still open).
- Retrospective state: Sprint 20 has three open issues (#122/#123/#124), so close-or-remediate review is not yet eligible.
- Roadmap horizon: 9 ordered open milestones before and after this run (Sprint 20 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was needed.
- Next eligible action: implement #122 (complete the parent delegation step/run from the linked child run's terminal outcome). Final target `main`; after this MEMORY handoff commit, the worktree is clean except for the preserved unrelated untracked `.claude/` directory.

---

- Run: 2026-07-14T19:37:04Z — implementation run (scheduled).
- Active milestone at start: Sprint 19 "Web approval and intervention controls" (#19). Selected #119 (priority:3, `agent-ready`), the sole open and eligible issue; its dependency #118 was closed.
- Completed: added separate `scripts/dashboard-approval-review.sh` rather than weakening Sprint 18's immutable review. It creates isolated `/tmp` state, lets a real worker execute a Docker preflight so the run reaches `running`, keeps that worker alive at a second command's approval gate, serves API/dashboard explicitly on loopback, verifies the API's actual `127.0.0.1` listening socket, and asserts the browser decision led to an approved request, succeeded step/run, exactly one guarded-step start/success, and terminal `run_succeeded`. DEVELOPMENT.md documents the exact confirmation, expected browser evidence, cleanup, and distinction from the read-only hash check. Added Plan 0109.
- Live UAT: the dashboard showed the real preflight succeeded and `approved-command-step` pending; Approve opened “Approve this step?”; Confirm approve added durable `step_approved`; the worker executed the guarded Docker command once; polling rendered both steps/run succeeded and the complete terminal history. The harness's durable assertions passed and browser logs had no warnings/errors.
- Verification: `sh -n scripts/dashboard-approval-review.sh`; real Docker/API/dashboard/worker/browser review; dashboard `pnpm test` 43 passed, `pnpm typecheck` clean, `pnpm build` clean; full `pytest` 760 passed; `codex-agentic-os index check` current; `git diff --check` clean. No indexed Python source changed.
- Durable state: implementation commit `006d26e` pushed to `origin/main`; #119 closed with commit and verification evidence.
- Retrospective: user-requested same-run close-or-remediate review created and closed #120. All four Sprint 19 exit criteria passed against issues #117–#119, offline tests, Decision 0008, DEVELOPMENT.md, and the real browser/worker/Docker review. Sprint 19 closed with no remediation; Sprint 20 "Parent-child run delegation" is now active with 0 open and 0 ready issues.
- Blocked review: repository-wide open `blocked` search is empty.
- Roadmap horizon: 10 ordered open milestones before retrospective closure (Sprint 19 through Sprint 28) and 9 after (Sprint 20 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was needed.
- Next eligible action: replenishment-only review for Sprint 20 against its parent-child delegation objective and exit criteria. Final target `main`; after this MEMORY handoff commit, the worktree is clean except for the preserved unrelated untracked `.claude/` directory.

---

- Run: 2026-07-14T19:12:00Z — implementation run (scheduled).
- Active milestone at start: Sprint 19 "Web approval and intervention controls" (#19), unchanged from the prior run. Confirmed GitHub state matched the prior run's `MEMORY.md` record exactly: #118 (priority:2, `agent-ready`) was the sole unblocked issue; #119 remained correctly `blocked` on it. Selected #118.
- Completed: added contextual, confirmed dashboard mutation controls calling the #117 API. `lib/api.ts` gained `RunStep.failure_kind`/`retry_eligible` and `approveStep`/`rejectStep`/`cancelRun`/`retryStep` client functions (a `postJson` helper parallel to the existing `fetchJson`). `app/api/v1/runs/[...segments]/route.ts` gained a `POST` handler (backed by a new `proxyMutationPost` in `proxy.ts`) that forwards only the four mutation-shaped paths (`{run_id}/cancel`, `{run_id}/steps/{step_id}/{approve,reject,retry}`); every other path 404s without forwarding, mirroring the existing `GET` handler's boundary. `run-detail.tsx` renders approve/reject only next to a pending approval, cancel only next to an active (`queued`/`running`) run, and retry only on a failed step whose `retry_eligible` is `true`, each behind a `ConfirmMutationButton` (a local wrapper around `AlertDialogTrigger`/`AlertDialogAction` with its own controlled `open` state, since this codebase's `AlertDialogAction` is a plain `Button`, not a `Close` primitive, so the confirm handler must both mutate and close the dialog itself). `hooks/use-polling-load.ts` now returns `{ state, refresh }` instead of bare `LoadState<T>` (`run-list.tsx` updated to destructure `{ state }`); `refresh()` invokes the in-effect `poll` closure immediately via a ref, clearing any pending timeout first, so a mutation's post-attempt reload doesn't wait out the polling interval and doesn't race a concurrently firing scheduled poll. Every mutation attempt — success or failure — triggers `refresh()` and renders the durable outcome; failures show a destructive `Alert` banner with the API's structured error message rather than guessing. Added Plan 0108.
- Verification: `pnpm test` 43 passed (up from 23 at a `git stash` baseline, +20 net new/changed cases across `lib/api.test.ts`, new `hooks/use-polling-load.test.ts`, `app/api/v1/runs/[...segments]/route.test.ts`, `components/run-detail.test.tsx`), `pnpm typecheck` clean, `pnpm build` clean. Full `pytest` remained 760 passed (no Python source changed); `codex-agentic-os index check` current (dashboard/TypeScript files are not indexed); `git diff --check` clean. Live UAT: built an isolated state DB (`agent register`/`run create`/`run add-step --approval-required`/`run transition running`) and served it through `codex-agentic-os api serve` + the dashboard dev server via the repository's existing `.claude/launch.json` `api`/`dashboard` preview configs (port 8099/3010); in a real browser, clicked Approve, confirmed via the dialog, watched the pending-approval card clear and lifecycle history gain `step_approved`, confirmed durably via `run inspect` (step revision incremented outside the browser); clicked Cancel run, confirmed, watched the run/step move to `cancelled` and the Cancel control disappear once terminal; replayed a direct `POST .../approve` against the now-decided step via `curl` and confirmed the existing 409 contract unchanged; no browser console errors. Updated `DEVELOPMENT.md`'s dashboard section to correct the now-inaccurate Sprint 18 "no mutation controls" claims and added a short "Dashboard mutation controls" subsection.
- Durable state: implementation commit `be2b53c` pushed to `origin/main`; #118 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search found only #119 (blocked on #118, now closed). Removed `blocked` and added `agent-ready` to #119 with an evidence comment describing the live confirmation-dialog text/contract it will exercise. Sprint 19 now has one open issue (#119), which is also its only ready issue.
- Retrospective state: Sprint 19 still has one open issue (#119), so close-or-remediate review is not yet eligible.
- Roadmap horizon: 10 ordered open milestones before and after this run (Sprint 19 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was needed.
- Next eligible action: implement #119 (repository-owned end-to-end review demonstrating a browser approval completing a real worker-executed run, building on the now-live dashboard controls). Final target `main`; after this MEMORY handoff commit, the worktree is clean except for the preserved unrelated untracked `.claude/` directory.

---

- Run: 2026-07-14T18:35:25Z — implementation run (scheduled).
- Active milestone at start: Sprint 19 "Web approval and intervention controls" (#19). GitHub already showed issues #117-#119 created 2026-07-14T18:08-18:09Z — a replenishment pass whose MEMORY.md record was never committed (mirroring the same gap the 2026-07-14T15:10:00Z run recorded for #112-#115; the prior run's actual last commit was `1dec2c9`, the Sprint 18 retrospective closure, with nothing after it). Treated GitHub as authoritative and selected #117 (priority:1, agent-ready, no stated dependency); #118/#119 remained correctly `blocked`.
- Completed: added four `POST` mutation routes to `src/codex_agentic_os/api.py` — `/api/v1/runs/{run_id}/steps/{step_id}/{approve,reject,retry}` and `/api/v1/runs/{run_id}/cancel` — delegating to the existing durable `RunCoordinator.approve_step`/`reject_step`/`retry_step`/`cancel` compare-and-swap operations. Each mutation opens a writable state connection derived from the read-only coordinator's own `store.path` only for the duration of one request; every `GET` route is unchanged and stays read-only, so `build_server`'s public signature and every existing call site (production `cli.py` and ~90 pre-existing tests) needed no changes. Renamed the private handler `_ReadOnlyAPIRequestHandler` to `_APIRequestHandler` (no longer purely read-only). Success responses reuse `_run_payload` + `_redact_step_for_http` (same as `GET` run detail), so Decision 0008's redaction applies identically to mutation responses — verified with a dedicated test. Failures map to structured JSON: 404 unknown run/step, 400 malformed/missing body fields (retry requires integer `expected_step_revision`/`expected_run_revision`, mirroring the CLI's required flags; the new retried step's id is generated server-side), 409 ineligible state or genuine CAS conflict. No `agent_id`/multi-operator identity is accepted, per Sprint 19's explicit scope boundary. Updated `cli.py`'s `api`/`api serve` help text and `DEVELOPMENT.md`'s API section to describe the mutation contract (no code change needed there — `api serve` already passes its `--state-db` coordinator straight to `build_server`). Added Plan 0107.
- Verification: activated `.venv`; full `pytest` 760 passed (up from 720; `test_api.py` alone 88 passed, +40 net new/changed cases covering success, ineligibility, a genuine CAS conflict, malformed input, unknown run/step, cross-run step mismatch, unsupported methods, and redaction parity on mutation responses); `codex-agentic-os index build` (source changed) then `index check` current at 27 files, 1101 symbols, 6596 relationships; `git diff --check` clean. No frontend files changed (dashboard UI controls are #118, out of scope).
- Durable state: implementation commit `f4a63bf` pushed to `origin/main`; #117 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search found only #118 (blocked solely on #117, now closed) and #119 (blocked on #118, still open). Removed `blocked` and added `agent-ready` to #118 with an evidence comment pointing at the four new mutation route contracts; #119 remains correctly `blocked` on #118. Sprint 19 now has one ready issue: #118.
- Retrospective state: Sprint 19 still has two open issues (#118/#119), so close-or-remediate review is not yet eligible.
- Roadmap horizon: 10 ordered open milestones before and after this run (Sprint 19 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was needed.
- Next eligible action: implement #118 (dashboard contextual approve/reject/cancel/retry controls with confirmation and durable-state refresh, built on the now-live mutation API). Final target `main`; after this MEMORY handoff commit, the worktree is clean except for the preserved unrelated untracked `.claude/` directory.
