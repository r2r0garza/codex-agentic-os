# Automation Memory

- Run: 2026-07-15T01:38:07Z — implementation run (scheduled).
- Active milestone at start: Sprint 22 "Bounded agentic tool loop" (#22). Selected #133 (priority:2, `agent-ready`), the sole unblocked issue; #134 was correctly `blocked` on it.
- Completed: `execute_next_step` now distinguishes a pre-existing `running` tool-declaring step's two possible boundaries. A step whose last durable iteration is still `requested` remains genuinely uncertain and keeps failing definitively through the existing `run recover` contract (unchanged). A step whose last iteration is `executed` is a safe, fully durable boundary (guaranteed by the pre-existing invariant that only the final stored iteration may be incomplete): a replacement worker resumes through new `_resume_tool_loop`, which replays every completed iteration's assistant/tool turns from stored evidence via `_replay_tool_iteration_messages` and issues the next provider request, never re-executing an already-durable sandboxed command. Extracted the shared tool-call loop body (fresh dispatch and resume both now call it) into `_advance_tool_loop` so budget, undeclared-tool, and sandbox-resolver handling stay identical for both paths. Added cancellation-between-iterations detection: proactively, the top of every loop pass re-checks the run/step before acting on a new provider response; reactively, the two CAS-guarded persist helpers check a write conflict for a concurrent cancellation and raise an internal `_ToolLoopCancelled` instead of a generic conflict error. Either path stops the loop with no further provider call or sandbox execution, leaving completed iterations exactly as durably recorded. `recover_running_step` itself is unchanged — still the operator's explicit, unconditional fail path for cases the automatic resume does not cover. Added Plan 0120 and updated DEVELOPMENT.md.
- Verification: activated `.venv`; 4 new focused runtime tests (resume after executed-phase interruption without re-executing the tool, resume replaying completed-iteration context, cancellation via the proactive check, cancellation via the reactive CAS-conflict path) plus 1 new worker test (a fresh `run_worker` process completing a multi-iteration tool task after a simulated crash using only durable state); full `pytest` passed 858 (up from 853); `codex-agentic-os index build` refreshed 27 files / 1328 symbols / 7732 relationships; `index check` current; `git diff --check` clean.
- Durable state: implementation commit `26c6bf2` pushed to `origin/main`; #133 auto-closed by GitHub on push (commit message included "Closes #133"), verification evidence added as a follow-up comment since it was already closed by the time the close-with-comment call ran.
- Blocked review: repository-wide open `blocked` search found only #134 (blocked on now-closed #132 and #133). Removed `blocked` and added `agent-ready` with an evidence comment naming the resume/cancellation contract (#133, Plan 0120) its per-iteration history/redaction review builds on. No other blocked issues found repository-wide.
- Retrospective state: Sprint 22 has one open issue (#134), so close-or-remediate review is not yet eligible.
- Roadmap horizon: 21 ordered open milestones before and after this run (Sprint 22 through Sprint 42), above the three-sprint threshold; no planning handoff was needed.
- Next eligible action: implement #134 (record and review per-iteration tool-loop history — durable per-iteration tool activity in history under the redaction contract, plus a committed multi-iteration worker-restart review script). Final target `main`; after this MEMORY handoff commit, the worktree is clean except for the preserved unrelated untracked `.claude/` directory.

---

- Run: 2026-07-15T00:38:07Z — implementation run (scheduled).
- Active milestone at start: Sprint 22 "Bounded agentic tool loop" (#22). Selected #132 (priority:1, `agent-ready`), the sole unblocked issue; #133/#134 were correctly `blocked` on it and on later recovery work.
- Completed: replaced the single mutable `tool_call` path with durable ordered `RunStep.tool_iterations`. Each iteration persists the normalized provider response and requested call before sandbox execution, then replaces that entry with the sanitized executed command/result before the next provider request. Completed assistant/tool turns replay in order until a final response succeeds. The explicit budget bounds sandbox executions; a further model call is persisted as `rejected_budget`, fails the step/run definitively, and executes no additional command. Undeclared responses are likewise persisted as `rejected_undeclared`. Trusted inspection exposes the full sequence and retains `tool_call` as the latest-call compatibility alias; legacy records upgrade to a one-entry typed sequence on read. Loopback HTTP redacts per-iteration response content/raw evidence plus call arguments, command, stdout, and stderr. Added Plan 0119 and updated DEVELOPMENT.md.
- Verification: activated `.venv`; focused runtime/API checks passed 15 tests; all runtime tests passed 296; CLI/API/worker/chat passed 403; full `pytest` passed 853; `codex-agentic-os index build` refreshed 27 files / 1289 symbols / 7611 relationships; `index check` current; `git diff --check` clean.
- Durable state: implementation commit `7cd5ba6` pushed to `origin/main`; #132 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` review found #133 and #134. Removed `blocked` and added `agent-ready` to #133 because #132 supplied its durable ordered phase record; #134 remains correctly blocked on #133. Sprint 22 has one ready issue: #133.
- Retrospective state: Sprint 22 still has open #133/#134, so close-or-remediate review is not yet eligible.
- Roadmap horizon: 21 ordered open milestones before and after this run (Sprint 22 through Sprint 42), above the three-sprint threshold; no planning handoff was needed.
- Next eligible action: implement #133 (resume and cancel bounded tool loops safely between iterations). Final target `main`; after this MEMORY handoff commit, the worktree is clean except for the preserved unrelated untracked `.claude/` directory.

---

- Run: 2026-07-15T00:00:00Z — implementation run (scheduled).
- Active milestone at start: Sprint 22 "Bounded agentic tool loop" (#22). A concurrent session had already replenished it with #131–#134 since the prior run's zero-issue snapshot; selected #131 (priority:1, `agent-ready`), the sole unblocked issue — #132/#133/#134 were correctly `blocked` on it and on each other.
- Completed: added a durable `RunStep.tool_iteration_budget`, required and validated (explicit positive integer) before any run/step mutation whenever a provider step declares tools, and rejected for steps that declare no tools with the same error shape as `sandbox_policy`. `_validate_tool_iteration_budget` takes a `require_explicit` flag so `add_step` fails closed on a missing budget (no default unlimited loop) while `_step()`'s record-parsing path tolerates a durable record persisted before this field existed (tools declared, no budget) rather than failing to load. Preserved the budget across every existing `tool_declarations` rewrite path by adding one line inside each of their identical `if step.tool_declarations:` blocks: cancel closure, `start_next_step`, both tool-call phase writes (`_running_provider_step_payload`), `transition_step`, `complete_step_from_result` (defensive; command steps can't declare tools), `complete_step_from_chat_response`, `fail_step_from_error`, `recover_running_step`, `_decision_payload`, and `state.py`'s `retry_failed_step` preserved-key list. Extended `run add-step` with `--tool-iteration-budget COUNT`; `run inspect`/`run inspect-step` show it for tool-declaring steps and omit it otherwise. Updated several Plan 0114/0116/0117 tests that created tool-declaring steps without a budget. Added Plan 0118 and documented the contract in DEVELOPMENT.md.
- Verification: activated `.venv`; 9 new focused runtime tests (missing/invalid budget rejection across zero/negative/float/bool/string, budget-without-tools rejection, retry preservation, legacy no-budget record read-back) plus updated CLI/worker/runtime tests supplying the now-required budget; full `pytest` passed 852 (up from 843); `codex-agentic-os index build` refreshed 27 files / 1276 symbols / 7555 relationships; `index check` current; `git diff --check` clean.
- Durable state: implementation commit `fba3081` pushed to `origin/main`; #131 auto-closed by GitHub on push (commit message included "Closes #131"), verification evidence added as a follow-up comment since `gh issue close --comment` skipped posting it once already closed.
- Blocked review: repository-wide open `blocked` search found #132 (blocked solely on now-closed #131), #133 (blocked on #132), and #134 (blocked on #132 and #133). Removed `blocked` and added `agent-ready` to #132 with an evidence comment naming the durable `tool_iteration_budget` contract (#131, Plan 0118) its bounded loop consumes. #133 and #134 remain correctly `blocked`.
- Roadmap horizon: 21 ordered open milestones before and after this run (Sprint 22 through Sprint 42), above the three-sprint threshold; no planning handoff was needed.
- Next eligible action: implement #132 (persist and enforce bounded iterative tool execution as a durable ordered loop). Final target `main`; after this MEMORY handoff commit, the worktree is clean except for the preserved unrelated untracked `.claude/` directory.

---

- Run: 2026-07-14T23:39:15Z — implementation plus close-or-remediate review (scheduled).
- Active milestone at start: Sprint 21 "Durable model tool calling" (#21). Selected #129 (priority:2, `agent-ready`), the sole open and ready issue.
- Completed: extended ordered run history with safe `tool_name`/`tool_outcome` evidence for requested calls, sandbox results (`succeeded`/`failed` from exit status), and undeclared-call rejection. An undeclared request now records `rejected_undeclared` atomically with definitive step/run failure, never invokes the sandbox, and remains retry-eligible. Fixed the generic retry copier to preserve a tool step's declarations and optional response-artifact name while deliberately dropping the prior attempt's `tool_call`. Trusted local CLI inspection continues to reconstruct declarations, arguments, durable phase, sandbox result, and final response; loopback HTTP now applies Decision 0008 to tool declaration commands plus call arguments, executed argv, stdout, and stderr. Added Plan 0117, DEVELOPMENT guidance, and `scripts/tool-call-history-review.sh`.
- Verification: activated `.venv`; focused state/runtime/CLI/API files passed 43/286/250/89 tests; retrospective adapter-tool selection passed 14 tests; full `pytest` passed 843; the Docker-backed tool-call review passed twice and reconstructed the full single-round flow from fresh CLI processes; index rebuilt to 27 files / 1270 symbols / 7511 relationships and `index check` current; `git diff --check` clean.
- Durable state: implementation commit `04448ed` pushed to `origin/main`; #129 closed with commit and verification evidence. Created and closed retrospective #130; all five Sprint 21 exit criteria passed against #126–#129, commits `50ad5c4`/`5ccd7d1`/`70feec2`/`04448ed`, Plans 0114–0117, Decision 0008, focused/full tests, documentation, and the Docker operator review. Sprint 21 closed with no remediation.
- Blocked review: repository-wide open `blocked` search is empty. Sprint 22 "Bounded agentic tool loop" is now active with zero open and zero `agent-ready` issues.
- Roadmap horizon: 22 ordered open milestones before Sprint 21 closure and 21 after (Sprint 22 through Sprint 42), above the three-sprint threshold; no planning handoff was needed.
- Next eligible action: replenishment-only review for Sprint 22 against its bounded agentic tool-loop objective and exit criteria. Final target `main`; after this MEMORY handoff commit, the worktree is clean except for the preserved unrelated untracked `.claude/` directory.

---

- Run: 2026-07-14T23:10:00Z — implementation run (scheduled).
- Active milestone at start: Sprint 21 "Durable model tool calling" (#21). Selected #128 (priority:1, `agent-ready`), the sole unblocked issue; #129 was correctly `blocked` on it.
- Completed: extended the provider-neutral `ChatMessage`/`ChatResponse` with a normalized `ChatToolCall`, mapped it into and out of the OpenAI-compatible, Anthropic, and Google native payload shapes (tool-call parsing plus assistant/tool follow-up-turn mapping), and rejected more than one tool call per response. Added a durable `ToolCallRecord`/`ToolCallPhase` and `RunStep.tool_call` written in two phases (`requested` before the sandboxed command runs, `executed` once its result is durable), preserved across every existing step-payload rewrite path. `execute_next_step` now detects a tool call in the adapter response, executes the declared command template unmodified (model-requested arguments are recorded as evidence only, never interpolated into the executed argv) through the step's persisted sandbox policy, and issues the single follow-up request; an undeclared tool name, a missing sandbox resolver at call time, or a second tool call in the follow-up response all fail the step definitively via the existing `fail_step_from_error` path without executing a command. Recovery-safety needed no new state machine: `recover_running_step` never resumes a step, only fails it, so preserving `tool_call` in that failure payload (whichever phase was last durably written) is sufficient to guarantee no silent re-execution. Fixed `cli.py`'s `run execute-next`, which previously never passed a `sandbox_resolver` for any provider-message step, to pass the persisted-policy resolver when the next step declares tools; `worker.py`'s `run_worker` already passed both resolvers unconditionally and needed no change. `run inspect`/`run inspect-step` surface `tool_call` when present. Added Plan 0116 and documented the contract in DEVELOPMENT.md.
- Verification: activated `.venv`; 9 new chat adapter tests, 11 new runtime tests (end-to-end happy path, phase-ordering-before-execution, env redaction, undeclared-tool/missing-resolver/second-call failures, and interruption/recovery without re-execution at each phase), 1 new worker test, and 1 new CLI end-to-end test; full `pytest` 841 passed (up from 822); `codex-agentic-os index build` refreshed 27 files / 1262 symbols / 7485 relationships; `index check` current; `git diff --check` clean.
- Durable state: implementation commit `70feec2` pushed to `origin/main`; #128 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search found only #129 (blocked solely on now-closed #128). Removed `blocked` and added `agent-ready` with an evidence comment naming the tool-execution/recovery contract (#128, Plan 0116) it builds durable-history "tool activity" recording and the committed end-to-end review script on. No other blocked issues found repository-wide.
- Retrospective state: Sprint 21 has one open issue (#129), so close-or-remediate review is not yet eligible.
- Roadmap horizon: 22 ordered open milestones before and after this run (Sprint 21 through Sprint 42), above the three-sprint threshold; no planning handoff was needed.
- Next eligible action: implement #129 (record and review durable tool-call failures and history — undeclared-tool history entries, redacted tool-activity history, and a committed end-to-end review script). Final target `main`; after this MEMORY handoff commit, the worktree is clean except for the preserved unrelated untracked `.claude/` directory.
