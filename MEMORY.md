# Automation Memory

- Run: 2026-07-14T05:32:16Z — retrospective run.
- Active milestone at start: Sprint 13 "Operator-accepted model plan decomposition" (#13). All five implementation issues (#87-#91) were closed, so no implementation issue was selected; created retrospective issue #92.
- Completed: exercised the accepted and rejected operator workflows end to end, reviewed the four milestone exit criteria against Plans 0084-0088, DEVELOPMENT.md, Decisions 0005/0006, committed tests, and current index evidence. All criteria passed: durable provider-proposed drafts preserve malformed evidence and queue nothing before acceptance; `run inspect-plan` is read-only; accept/reject is CAS-safe and atomically materializes all steps or none with plan/agent provenance; accepted command/provider execution and rejected-plan state both reconstruct across fresh process/state connections. #92 records the full retrospective and is closed; Sprint 13 is closed with no remediation milestone.
- Verification: activated `.venv`; focused operator review `2 passed in 0.17s`; full suite `579 passed in 7.01s`; `codex-agentic-os index check` current (23 files, 875 symbols, 5090 relationships). Final `git diff --check` and branch-state checks are part of the handoff commit.
- Durable state: implementation evidence remains product commit `16059a2` on `origin/main`; retrospective issue #92 and milestone #13 were closed in GitHub. This run changes only this durable MEMORY record and commits/pushes it on `main`.
- Blocked review: repository-wide open `blocked` search found none. Resulting active Sprint 14 has 0 open and 0 ready issues.
- Roadmap horizon: 16 ordered open milestones before retrospective closure (Sprint 13 through Sprint 28), 15 after (Sprint 14 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was created.
- Next eligible action: replenishment-only review for Sprint 14 "Capability-based provider routing"; create only issues that close its stated exit-criteria gaps, without implementing one in the same run. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.

---

- Run: 2026-07-13T00:00:00Z — implementation run (scheduled).
- Active milestone: Sprint 13 "Operator-accepted model plan decomposition" (#13). Selected #90 (priority:3), the sole `agent-ready` issue and the milestone's last open issue.
- Completed: no runtime/CLI product code changed — all primitives (`run plan`, `run inspect-plan`, `run accept-plan`/`run reject-plan`, `run execute-next`, `run inspect`, `run history`) already existed from Plans 0084-0087. Closed the milestone's remaining "demonstrate end to end" exit criterion with committed, executable proof: added `test_cli_end_to_end_operator_review_reconstructs_plan_execution_after_restart` (create → plan via fake offline adapter → inspect-plan → pre-acceptance execute-next proves `{"attempted": false}`/no draft step runs before acceptance → accept-plan with a registered agent → two execute-next dispatches through the unchanged `ContainerSandbox`/adapter-resolver worker path → fresh `RunCoordinator`/`StateStore` connections and fresh CLI calls reconstruct objective, accepted draft, both executed steps, terminal `succeeded` status, and the full `created→plan_accepted→run_started→step_started→step_succeeded→step_started→step_succeeded→run_succeeded` history with `plan_id`/`agent_id` provenance) and `test_cli_rejected_plan_remains_reconstructable_with_no_executable_steps_after_restart` (propose → reject → simulated restart → draft still shows `rejected`/`decision_agent_id`, execute-next still reports no attempt, history reconstructs `created→plan_rejected`). Learned `run accept-plan`/`run reject-plan --agent-id` require a registered agent (unlike `run claim`/`run add-step --agent-id`), and accepted steps carry a persisted sandbox policy so `run execute-next` must omit `--sandbox`. Added a DEVELOPMENT.md pointer to both tests instead of duplicating a manual walkthrough. Added Plan 0088.
- Tests: full suite `579 passed` (up from 577, +2 new end-to-end/regression tests).
- Verification: activated `.venv`; rebuilt index and confirmed current (23 files, 875 symbols, 5090 relationships); `git diff --check` clean.
- Durable state: implementation commit `16059a2` pushed to `origin/main`; #90 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search found none. Sprint 13 now has 0 open issues (#87-#91 all closed) — ready for a retrospective run.
- Roadmap horizon: 16 ordered open milestones (Sprint 13 through Sprint 28), above the three-sprint threshold; no planning handoff was needed.
- Next eligible action: run Sprint 13's retrospective (all issues closed, exit criteria ready for end-to-end review) — do not select a new implementation issue until the retrospective closes or remediates the milestone. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.

---

- Run: 2026-07-14T04:39:02Z — implementation run.
- Active milestone: Sprint 13 "Operator-accepted model plan decomposition" (#13). Selected #89 (priority:2), the sole unblocked `agent-ready` issue; #90 was blocked on #87/#88/#89.
- Completed: added CAS-safe `RunCoordinator.accept_plan`/`reject_plan` and `run accept-plan`/`run reject-plan`. A dedicated SQLite transaction checks the draft and attached run status/revision, rejects any existing materialized step id, allocates stable positions while holding the write lock, and atomically writes either all accepted queued steps plus the terminal plan state/history or no steps. Rejection records provenance and creates no steps. Added nullable/migrated `run_history.plan_id`, optional registered deciding-agent provenance, terminal `PlanDraft` inspection fields, DEVELOPMENT guidance, and completed Plan 0086. Accepted steps use the unchanged queued execution lifecycle.
- Tests: added runtime/CLI coverage for accept/reject success, stale revision, concurrent competing decisions with one winner, existing-step collision rollback/no partial materialization, invalid/missing/already-decided drafts, restart reconstruction, and execution through the existing lifecycle.
- Verification: activated `.venv`; full suite `577 passed` (up from 565); rebuilt index and confirmed current (23 files, 866 symbols, 5035 relationships); `git diff --check` clean.
- Durable state: implementation commit `ac847e2` pushed to `origin/main`; #89 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search found only #90. All three dependencies (#87/#88/#89) are now closed, so `blocked` was removed and `agent-ready` added with an evidence comment. Sprint 13 now has one ready issue: #90 (priority:3).
- Roadmap horizon: 16 ordered open milestones before and after (Sprint 13 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was created.
- Next eligible action: execute #90's end-to-end objective → proposal → inspection → acceptance → execution → restart/reconstruction review. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.

---

- Run: 2026-07-14T04:00:00Z — implementation run.
- Active milestone at start: Sprint 13 "Operator-accepted model plan decomposition" (#13), holding 3 open issues: #91 (priority:1, sole `agent-ready`, filed directly by the operator to unblock #89's Plan 0086 blocker), #89 (`blocked` on the same gap), #90 (`blocked` on #87/#88/#89). Selected #91, the sole `agent-ready` issue.
- Completed: extended `PlanStepProposal` (`src/codex_agentic_os/runtime.py`) with `step_id`, `command`, `timeout`, `sandbox_policy`, and `message` fields mirroring `RunStep`'s executable-input shape, closing the exact gap Plan 0086 recorded. `_parse_plan_proposal` now takes `plan_id`, validates each step's executable payload by reusing the existing `_validate_command`/`_validate_sandbox_policy`/`_validate_message` (the same rules `add_step` enforces — no duplicated logic), and materializes `step_id` deterministically as `f"{plan_id}-step-{index+1}"` (never model-supplied; collision-free within one draft by construction since `plan_id` is already unique and position is unique per draft). A command step requires `command` and a persisted `sandbox_policy` (matching the worker's real dispatch path, which requires a persisted policy and exposes no override flag) and must not include `message`; a provider step requires `message` and must not include `command`/`timeout`/`sandbox_policy`. Added `_plan_step_proposal_payload`/`_plan_step_proposal` to persist/reconstruct the new fields, reusing `_step()`'s exact validator-reuse pattern. Updated `PLAN_PROPOSAL_SYSTEM_PROMPT` and the CLI's `_plan_draft_payload` (new `_plan_step_proposal_payload` helper mirroring `_step_payload`'s field-omission convention). For "durable plan-decision history identity" (also in scope), deliberately added no new schema: the already-unique, already-retrievable `plan_id` satisfies it without an unused `run_history` column ahead of a concrete writer (documented rationale in Plan 0087, so #89 can cite `plan_id` directly when it lands). Accept/reject itself remains unimplemented (explicitly out of scope per #91). Added Plan 0087.
- Verification: activated `.venv`; updated all existing `run plan`/`run inspect-plan`/`propose_plan`/`get_plan` tests for the new required shape (fixtures now include `command`+`sandbox_policy` or `message`); added malformed-payload cases (missing command, missing sandbox policy, missing message, mismatched fields per execution kind, empty command, invalid sandbox kind), a step-id determinism/collision-freedom test, and an after-restart reconstruction test. Full suite `565 passed` (up from 555, +10 net). Rebuilt index (was stale after the source changes): now current (23 files, 838 symbols, 4849 relationships). `git diff --check` clean.
- Durable state: commit `dc8a820` pushed to `origin/main`; issue #91 closed with a verification comment.
- Blocked review: repository-wide `blocked`-label search found #89 and #90. #89's blocker (the Plan 0086 materialization gap) is resolved by this run, so `blocked` was removed and `agent-ready` restored with an evidence comment — also noting a remaining specification detail for #89's own run: acceptance must still check a materialized `step_id` for collision against already-existing `step` records (manual `add_step` or an earlier-accepted plan) before mutating, since only within-draft collision-freedom was guaranteed here. #90 remains correctly `blocked` (still depends on open #89). Sprint 13 now has one ready issue: #89 (priority:2).
- Roadmap horizon: 16 open milestones before and after this run (Sprint 13 through Sprint 28), above the three-sprint threshold, so no planning handoff was needed.
- Next eligible action: implement #89 (CAS-safe accept/reject) in Sprint 13, per the specification note left on its issue. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.

---

- Run: 2026-07-14T03:34:00Z — implementation selection stopped on an observed specification blocker.
- Active milestone: Sprint 13 "Operator-accepted model plan decomposition" (#13), holding two open issues at start: #89 (priority:2, sole `agent-ready`) and #90 (`blocked` on #87/#88/#89). Selected #89 under implementation mode; no other issue was implemented.
- Blocker: the durable proposal contract established by #87/Plan 0084 stores only each natural-language `objective` and `execution_kind` (`command`/`provider`), while the existing queued-step contract requires exactly one concrete command argv or complete `ProviderMessage`, with command dispatch additionally requiring an explicit persisted sandbox policy. #89 expressly excludes plan editing, so acceptance has no authorized source for those fields. Converting natural language into shell, inventing no-op commands, or silently changing execution kind would violate the established safety/execution contracts. Drafts also lack materialized step ids, and `run_history` has no `plan_id` identity for reconstructing decisions across multiple drafts. Recorded the bounded resume path in Plan 0086; no runtime/state/CLI/documentation/test/index source changed.
- Verification: activated `.venv`; full suite `555 passed`; `codex-agentic-os index check` current (23 files, 829 symbols, 4804 relationships); `git diff --check` clean.
- Durable state: commit `46bdfa8` pushed to `origin/main`; #89 remains open, received an evidence comment, lost `agent-ready`, and gained `blocked`. No issue or milestone was closed.
- Blocked review: #89 remains blocked until its executable proposal/materialization shape, collision-free step identity rule, and plan-decision history identity are specified. #90 remains correctly blocked because #89 is open. Sprint 13 now has 0 ready issues.
- Roadmap horizon: 16 open milestones before and after (Sprint 13 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was created.
- Next eligible action: remediate #89's specification inside Sprint 13, then remove `blocked`/restore `agent-ready`; do not begin #90 meanwhile. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.
