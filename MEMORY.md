# Automation Memory

- Run: 2026-07-14T16:36:09Z — implementation run (scheduled).
- Active milestone: Sprint 18 "Read-only operator web dashboard" (#18). Selected #115 (priority:3), the sole unblocked `agent-ready` issue; dependency #114 was closed.
- Completed: added `scripts/dashboard-operator-review.sh`, a repository-owned review harness that activates `.venv`, creates isolated durable state, registers and starts a real worker, executes the ordered command step through Docker, verifies the following provider step is pending approval, stops the worker, and serves the resulting state through the loopback API and dashboard. The harness verifies the database SHA-256 digest is unchanged while the read-only surfaces run. DEVELOPMENT.md now records the exact launch command, expected browser evidence, cleanup, and supported local overrides. Added Plan 0106.
- Verification: harness completed against a real Docker sandbox and both real servers; live in-app browser review showed the running mixed run, ordered succeeded command/queued provider steps, worker lifecycle provenance, visible pending approval, and explicit unavailable usage, with one navigation button and no forms or approve/reject/cancel/retry labels. Ctrl-C stopped the servers and the database hash remained unchanged. `sh -n scripts/dashboard-operator-review.sh`; dashboard `pnpm test` 23 passed, `pnpm typecheck` clean, `pnpm build` clean; full `pytest` 720 passed; `codex-agentic-os index check` current (27 files, 1072 symbols, 6388 relationships); `git diff --check` clean. No indexed Python source changed.
- Durable state: implementation commit `2bda009` pushed to `origin/main`; #115 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search is empty. Sprint 18 has 0 open and 0 ready issues.
- Retrospective state: Sprint 18 now has every issue closed and is eligible for a retrospective-only close-or-remediate run; this implementation run's selected mode remains fixed, so the retrospective is the next eligible action.
- Roadmap horizon: 11 ordered open milestones before and after this run (Sprint 18 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was needed.
- Next eligible action: retrospective review Sprint 18 against its dashboard exit criteria, then close or create evidence-based remediation. Final target `main`; worktree is dirty only for this MEMORY handoff update plus the preserved unrelated untracked `.claude/` directory.

---

- Run: 2026-07-14T16:00:00Z — implementation run (scheduled).
- Active milestone: Sprint 18 "Read-only operator web dashboard" (#18). Selected #114 (priority:2), the sole unblocked `agent-ready` issue; dependency #113 was closed.
- Completed: audited every dashboard component/client file for the six fields the Sprint 17 HTTP API redacts (declared `command`, `message.content`/`system`, captured `output.stdout`/`stderr`/`content`/`raw`) plus `usage.raw`; confirmed none are read or rendered anywhere (`run-detail.tsx` derives its step "command"/"provider" kind label only from whether `message` is present, never from its content). Added a `run-detail.test.tsx` regression that feeds a sentinel value into every one of those fields and asserts the sentinel never appears in the rendered DOM, guarding the boundary against a future regression rather than relying only on today's absence of a render call. Documented a "Running the dashboard against the API" section in DEVELOPMENT.md covering the paired `codex-agentic-os api serve` plus dashboard `pnpm dev` startup, `API_BASE_URL`/`.env.example`, and operator-facing verification expectations. Added Plan 0105.
- Verification: `pnpm test` 23 passed (up from 22, +1 net), `pnpm typecheck` clean, `pnpm build` clean; full `pytest` remained 720 passed (no Python source changed); `codex-agentic-os index check` current (27 files, 1072 symbols, 6388 relationships); `git diff --check` clean. Live UAT: registered an agent, created a real mixed run with a command step executed through a real Docker sandbox (captured stdout holding a sentinel secret) and an approval-required provider step (declared message holding a sentinel prompt), served it through a real `codex-agentic-os api serve` process and a real `pnpm dev` dashboard instance, and confirmed via the browser's network inspector that the JSON payload already carries `"<redacted>"` for the sensitive fields while the rendered page shows neither the sentinel text nor the `"<redacted>"` placeholder literal.
- Durable state: implementation commit `07b0fab` pushed to `origin/main`; #114 closed with commit and verification evidence.
- Blocked review: the only repository-wide open `blocked` issue was #115, which depended only on #114 (now closed); `blocked` was removed and `agent-ready` added with an evidence comment. Sprint 18 now has one ready issue: #115.
- Retrospective state: Sprint 18 still has one open issue (#115), so close-or-remediate review is not yet eligible.
- Roadmap horizon: 11 ordered open milestones before and after this run (Sprint 18 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was needed.
- Next eligible action: implement #115 (demonstrate a read-only browser review of a mixed run with a pending approval, building on the now-documented paired startup workflow). Final target `main`; after this MEMORY handoff commit, the worktree is clean except for the preserved unrelated untracked `.claude/` directory.

---

- Run: 2026-07-14T15:38:26Z — implementation run (scheduled).
- Active milestone: Sprint 18 "Read-only operator web dashboard" (#18). Selected #113 (priority:1), the sole unblocked `agent-ready` issue; dependency #112 was closed.
- Completed: added typed dashboard contracts and an allowlisted GET-only same-origin proxy for run detail, history, approvals, and usage; added run selection plus non-overlapping list/detail polling; rendered ordered steps, lifecycle history, pending approvals, and provider usage without mutation controls. A failed poll replaces stale ready data with the explicit API-unreachable state. Added Plan 0104.
- Verification: `pnpm test` 22 passed, `pnpm typecheck` clean, `pnpm build` clean; activated `.venv` and full `pytest` remained 720 passed; `codex-agentic-os index check` current (27 files, 1072 symbols, 6388 relationships); `git diff --check` clean. No Python source changed, so no index rebuild was needed.
- Durable state: implementation commit `d4cf4bd` pushed to `origin/main`; #113 closed with commit and verification evidence.
- Blocked review: the only repository-wide open `blocked` issues were #114 and #115. #114 depended only on #113, so `blocked` was removed and `agent-ready` added with an evidence comment; #115 remains correctly blocked on #114. Sprint 18 now has one ready issue: #114.
- Retrospective state: Sprint 18 still has two open issues (#114/#115), so close-or-remediate review is not yet eligible.
- Roadmap horizon: 11 ordered open milestones before and after this run (Sprint 18 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was needed.
- Next eligible action: implement #114 (audit the dashboard redaction presentation boundary and document the paired API/dashboard startup workflow). Final target `main`; after this MEMORY handoff commit, the worktree is clean except for the preserved unrelated untracked `.claude/` directory.

---

- Run: 2026-07-14T15:10:00Z — implementation run (scheduled).
- Active milestone at start: Sprint 18 "Read-only operator web dashboard" (#18). GitHub already showed four issues (#112-#115) created at 2026-07-14T14:47Z — a replenishment pass whose MEMORY.md record was never committed (the prior recorded run's "Next eligible action" was replenishment-only review, and no commit exists between `160e3ee` and this run introducing #112-#115). Treated GitHub as authoritative and selected #112 (priority:1, agent-ready, no stated dependency); #113/#114/#115 remain correctly `blocked` on it.
- Completed: scaffolded `dashboard/` (Next.js + shadcn/ui, full component set, `@tanstack/react-table`) per DEVELOPMENT.md, merged its ignore rules into the repository-root `.gitignore` scoped under `dashboard/`, and removed the scaffold's nested `.git/`. Added a typed client (`lib/api.ts`) and a read-only run-list view (`components/run-list.tsx`, rendered from `app/page.tsx`) covering loading, error, empty, and populated states with no mutation affordance. A live browser UAT against a real `codex-agentic-os api serve` instance found that a direct cross-origin browser fetch from the dashboard's port to the API's port fails (`net::ERR_FAILED`) because the API sends no CORS headers and issue #112 excludes API-server changes; added a same-origin Next.js proxy route (`app/api/v1/runs/route.ts`) that forwards `GET /api/v1/runs` to an operator-configured `API_BASE_URL` server-side (unaffected by CORS), verbatim status/body, plus `dashboard/.env.example`. Set up Vitest + React Testing Library per the bundled Next.js 16 docs (`node_modules/next/dist/docs/.../testing/vitest.md`). Added Plan 0103.
- Verification: `pnpm test` 13 passed; `pnpm typecheck` clean; `pnpm build` clean (pre-existing ESLint errors in scaffold-generated `components/ui/carousel.tsx`/`hooks/use-mobile.ts` are untouched by this issue and out of scope). Full Python suite unaffected: `720 passed`; `.code-index/` tracks only Python source so no rebuild was needed and `index check` stayed current (27 files, 1072 symbols, 6388 relationships); `git diff --check` clean. Real end-to-end UAT: seeded a temporary state database with two runs via the CLI, served it through a real `codex-agentic-os api serve` process, ran the dashboard dev server against it, and confirmed in a real browser the populated table (both runs, correct run id/objective/status/agent), the explicit "No runs yet" empty state against a freshly initialized empty database (no fabricated rows), and the explicit "Unable to reach the API" error state once the API process was stopped.
- Durable state: implementation commit `d2e954f` pushed to `origin/main`; #112 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search outside Sprint 18 is empty. #113 depended only on #112 (now closed), so `blocked` was removed and `agent-ready` added with an evidence comment pointing at the new `dashboard/app/api/v1/runs/route.ts` proxy convention #113 should extend for `/history`, `/approvals`, `/usage`. #114 (blocked by #113) and #115 (blocked by #114) remain correctly `blocked`. Sprint 18 now has one ready issue: #113.
- Retrospective state: Sprint 18 still has three open issues (#113/#114/#115), so close-or-remediate review is not yet eligible.
- Roadmap horizon: 11 ordered open milestones before and after this run (Sprint 18 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was needed.
- Next eligible action: implement #113 (dashboard run-detail view with polling and unreachable-API error state) in Sprint 18. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.

---

- Run: 2026-07-14T14:33:04Z — retrospective run (scheduled).
- Active milestone at start: Sprint 17.1 "HTTP declared-input redaction remediation" (#29). All milestone implementation issues were closed, so the run selected retrospective mode and created retrospective issue #111; no implementation issue was selected or created.
- Retrospective: every exit criterion passed. Commit `b760b21` and issue #109 redact declared command argv and provider `message.content`/`system` from HTTP run detail while retaining ordered steps and non-sensitive metadata; lifecycle and mixed-run regressions cover declared/captured redaction and the CLI retains full local detail. DEVELOPMENT.md and Decision 0008 match the corrected boundary. Created and closed #111 with the evidence, appended the passing disposition to milestone #29, and closed Sprint 17.1 with no remediation required.
- Verification: activated `.venv`; current full suite `720 passed in 10.96s`; `codex-agentic-os index check` reported current (27 files, 1072 symbols, 6388 relationships); `git diff --check` clean. Retrospective UAT evidence from #109 remains the real CLI-started loopback subprocess review against a temporary mixed database, confirming HTTP redaction, unchanged CLI detail, and clean SIGINT exit.
- Blocked review: repository-wide open `blocked` search is empty. Resulting active milestone Sprint 18 has 0 open and 0 ready issues.
- Roadmap horizon: 12 ordered open milestones before closure (Sprint 17.1, then Sprint 18 through Sprint 28) and 11 after (Sprint 18 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was needed.
- Next eligible action: replenishment-only review for Sprint 18 against its dashboard objective and exit criteria, creating only milestone-scoped executable slices. Final target `main`; the retrospective GitHub state is durable and this MEMORY update is ready for its handoff commit and push.
