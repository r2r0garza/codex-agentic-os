# Automation Memory

- Run: 2026-07-14T22:34:44Z — implementation run (scheduled).
- Active milestone at start: Sprint 21 "Durable model tool calling" (#21). Selected #127 (priority:1, `agent-ready`), the sole unblocked issue; #128/#129 were correctly `blocked`.
- Completed: extended the provider-neutral `ChatRequest` with ordered `ChatToolDeclaration` metadata and translated each persisted runtime declaration at dispatch without exposing its sandbox command template. OpenAI-compatible adapters now emit `tools[].function`, Anthropic emits native `tools[]` with `input_schema`, and Google emits ordered `functionDeclarations`. A missing parameters schema maps deterministically to an empty object schema; explicit non-object roots/properties fail before transport. Requests without tools omit the native field and preserve prior payload shapes. Exported the new request type, added Plan 0115, and updated DEVELOPMENT.md.
- Verification: activated `.venv`; 22 focused provider/runtime tool tests passed (7 new assertions across three native payloads, per-family schema rejection, and runtime command-redaction mapping); full `pytest` 822 passed (up from 815); `codex-agentic-os index build` refreshed 27 files / 1176 symbols / 7207 relationships; `index check` current; `git diff --check` clean.
- Durable state: implementation commit `5ccd7d1` pushed to `origin/main`; #127 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search found #128 and #129. #128 depended on now-closed #126/#127, so removed `blocked`, added `agent-ready`, and commented with the durable declaration/native mapping evidence. #129 remains correctly blocked on #128.
- Retrospective state: Sprint 21 has two open issues (#128/#129), so close-or-remediate review is not yet eligible.
- Roadmap horizon: 22 ordered open milestones before and after this run (Sprint 21 through Sprint 42), above the three-sprint threshold; no planning handoff was needed.
- Next eligible action: implement #128 (persist and execute one declared model tool-call round through the sandbox with recovery-safe phases). Final target `main`; after this MEMORY handoff commit, the worktree is clean except for the preserved unrelated untracked `.claude/` directory.

---

- Run: 2026-07-14T22:21:00Z — implementation run (scheduled).
- Active milestone at start: Sprint 21 "Durable model tool calling" (#21). Replenishment had already created #126–#129 in an earlier uncommitted run; selected #126 (priority:1, `agent-ready`), the sole unblocked issue — #127/#128/#129 were correctly `blocked`. A concurrent interactive session extended the roadmap to Sprint 42 mid-run (see the entry below); its changes were preserved untouched.
- Completed: added a durable `ToolDeclaration` model (name, command template, optional description, optional parameters schema) and `RunStep.tool_declarations`, so a provider-message step may declare named tools bound to a persisted sandboxed command template under the step's own `sandbox_policy`. Widened `_validate_sandbox_policy` (previously command-step-only) with a `has_tools` flag to allow a persisted policy on tool-declaring provider steps while preserving the existing rejection for provider steps without tools. Declarations validate before mutation (unique identifier-safe names via `str.isidentifier()`, non-empty command templates, provider-message-only scope, required persisted sandbox policy) and are preserved across every existing step-payload rewrite path that already preserves provider messages — cancel closure, `start_next_step`, `transition_step`, `_decision_payload`, `complete_step_from_result`, `fail_step_from_error`, `recover_running_step`, and `complete_step_from_chat_response`, which previously never carried a sandbox policy at all since provider steps couldn't have one before this change. `run add-step` gained a repeatable `--tool JSON` flag; `run inspect`/`run inspect-step` show `tool_declarations` and, when present, `sandbox_policy`, with both omitted exactly as before for steps declaring no tools. `PlanStepProposal` intentionally does not gain a tools field (out of the issue's stated scope). Added Plan 0114 and documented the contract in DEVELOPMENT.md.
- Verification: activated `.venv`; 20 new focused tests (13 runtime, 7 CLI) covering restart persistence, no-tool backward compatibility, sandbox-policy/provider-scope rejection, invalid-declaration rejection, CLI create/inspect parity and rejection paths without mutation, and preservation across a provider-step failure/reload; full `pytest` 815 passed (up from 793); `codex-agentic-os index build` refreshed 27 files / 1161 symbols / 7159 relationships; `index check` current; `git diff --check` clean.
- Durable state: implementation commit `50ad5c4` pushed to `origin/main`; #126 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search found #127 (blocked solely on now-closed #126), #128 (blocked on #126 and #127), and #129 (blocked on #128). Removed `blocked` and added `agent-ready` to #127 with an evidence comment naming the tool-declaration contract it maps into adapter payloads. #128 and #129 remain correctly `blocked`.
- Retrospective state: Sprint 21 has three open issues (#127/#128/#129), so close-or-remediate review is not yet eligible.
- Roadmap horizon: 22 ordered open milestones before and after this run (Sprint 21 through Sprint 42, per the concurrent roadmap-extension run below), far above the three-sprint threshold; no planning handoff was needed.
- Next eligible action: implement #127 (map declared tools into OpenAI-compatible/Anthropic/Google native tool payloads via offline transport tests). Final target `main`; after this MEMORY handoff commit, the worktree is clean except for the preserved unrelated untracked `.claude/` directory.

---

- Run: 2026-07-14T22:11:43Z — roadmap extension (interactive, user-authorized rolling-horizon exception).
- Authority and evidence: re-read `VISION.md`, the sprint-planning skill/template, current `MEMORY.md`, committed index manifest, relevant runtime/sandbox/API/dashboard evidence, every GitHub milestone through Sprint 28, the active Sprint 21 queue, and repository-wide blocked issues. The code index was current at the audit start, then became stale because a concurrent Sprint 21 implementation changed tracked Python source; those unrelated changes were preserved and not rebuilt or included here.
- Roadmap change: at the user's explicit request, extended the already-overfull open horizon beyond its normal active-plus-two contract. Created GitHub milestones Sprint 29 through Sprint 42: one-command local control plane (#30), objective-to-execution web workflow (#31), isolated repository workspaces (#32), crash-reconciled external execution (#33), bounded context management (#34), adaptive durable replanning (#35), agent profiles and capability-aware delegation (#36), reusable skills and MCP tools (#37), complete execution security policy (#38), cost budgets and usage governance (#39), selectable DeepAgents harness (#40), durable parallel fan-out and join (#41), runtime evaluation and harness conformance (#42), and state lifecycle operations (#43).
- Verification: all 14 milestones are open, uniquely ordered after Sprint 28, contain the required Objective / Exit criteria / Scope boundary / Dependencies / Rationale sections, and have zero issues. No active or future milestone was rewritten, no implementation issue was created, and no due date was assigned.
- Blocked review: Sprint 21 remains active with #126 as the sole `agent-ready` issue. #127, #128, and #129 remain correctly `blocked` on the earlier tool-declaration/execution slices; no planning evidence resolved those implementation dependencies.
- Remaining uncertainty: post-capstone milestone assumptions must be revalidated against retrospectives as each sprint approaches. In particular, Sprint 39 must pin and test the then-current DeepAgents adapter surface while keeping Codex Agentic OS authoritative for state, tools, policy, delegation, and recovery.
- Roadmap horizon: 8 ordered open milestones before (Sprint 21 through Sprint 28) and 22 after (Sprint 21 through Sprint 42), an explicit user-authorized exception to `VISION.md`'s three-milestone rolling horizon. Next active work remains Sprint 21 issue #126. Final target `main`; preserve the concurrent Sprint 21 source edits and unrelated untracked `.claude/` directory.

---

- Run: 2026-07-14T21:38:13Z — implementation run (scheduled).
- Active milestone at start: Sprint 20 "Parent-child run delegation" (#20). Selected #124 (priority:2, `agent-ready`), the sole open issue; #121/#122/#123 were closed.
- Completed: delegation history now persists `delegated_run_id` on the parent's `step_delegated` entry and `parent_run_id`/`parent_step_id` on the child's `created` entry through additive SQLite columns and the existing read-only CLI/API payload. Declaration rejects delegation to the current run's assigned agent or any assigned ancestor agent before step append, while leaving unassigned edges valid; malformed pre-existing parent cycles also reject. Added Plan 0113 and documented the contract.
- Live review: `scripts/delegation-interruption-review.sh` passed with two registered agents and a Docker command step: the parent worker dispatched the child, was terminated, the child worker completed, and a fresh parent-worker process reconciled the terminal outcome; final inspection and history assertions passed from both directions.
- Verification: activated `.venv`; 29 focused delegation tests passed; full `pytest` 793 passed (up from 791); refreshed index 27 files / 1146 symbols / 7040 relationships; `index check` current; `git diff --check` clean.
- Durable state: implementation commit `fb05826` pushed to `origin/main`; #124 closed with commit and verification evidence.
- Retrospective: created and closed #125. All four Sprint 20 exit criteria passed against #121–#124, Plans 0110–0113, Decisions 0005/0006, focused/full tests, DEVELOPMENT.md, and the real two-agent worker/Docker interruption review. Sprint 20 closed with no remediation; Sprint 21 "Durable model tool calling" is now active with 0 open and 0 ready issues.
- Blocked review: repository-wide open `blocked` search is empty.
- Roadmap horizon: 9 ordered open milestones before retrospective closure (Sprint 20 through Sprint 28) and 8 after (Sprint 21 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was needed.
- Next eligible action: replenishment-only review for Sprint 21 against its durable model tool-calling objective and exit criteria. Final target `main`; after this MEMORY handoff commit, the worktree is clean except for the preserved unrelated untracked `.claude/` directory.

---

- Run: 2026-07-14T21:10:00Z — implementation run (scheduled).
- Active milestone at start: Sprint 20 "Parent-child run delegation" (#20). Selected #123 (priority:2, `agent-ready`), the sole unblocked issue; its dependencies #121/#122 were closed. #124 remained blocked on it.
- Completed: hardened delegation recovery against orphaning a linked child. `RunCoordinator.cancel` now gathers a full cancel closure (parent, plus any active `queued`/`running` delegated child and that child's own active steps, recursively) into one atomic `StateStore.put_many` transaction via new helper `_collect_cancel_closure`, instead of only cancelling the parent's own steps and silently leaving an active child running unattended with no parent step left to reconcile it. A child already at a terminal status is left untouched. `RunCoordinator.recover_running_step` now rejects a running delegation step outright with a message pointing at the linked child run, since a delegation step's `running` status is a legitimate parked wait-on-child state, not the uncertain in-process command/provider execution that path is meant to fail explicitly. Both the cancel cascade and `_reconcile_delegation_step` now also verify a child run's `parent_run_id`/`parent_step_id` still points back to the delegating parent/step before acting on it, rejecting explicitly on a mismatch rather than silently cancelling or completing an unrelated run (defensive; no reachable coordinator path can currently produce a mismatch, since child run ids are always freshly minted via `{step_id}-child`). The pre-existing single-transaction atomic spawn (#121) and `DelegationPendingError` reconciliation path (#122) already closed the literal spawn/linkage interruption window, so neither needed changes. Updated DEVELOPMENT.md's cancel/recover/delegation sections and added Plan 0112.
- Verification: activated `.venv`; focused delegation runtime tests 22 passed (up from 16, +5 new: cascade-cancel with the child's own active step, terminal-child-left-untouched, cancel-path linkage-mismatch rejection, recover-running-step delegation rejection, reconcile-path linkage-mismatch rejection); full `pytest` 791 passed (up from 786); `codex-agentic-os index build` refreshed 27 files / 1143 symbols / 7016 relationships; `index check` current; `git diff --check` clean.
- Durable state: implementation commit `90b12e2` pushed to `origin/main`; #123 auto-closed by GitHub's "Closes #123" commit-message linking on push, with a follow-up comment adding full verification evidence since the auto-close consumed the `gh issue close --comment` attempt.
- Blocked review: repository-wide open `blocked` search found only #124 (blocked on #121/#122/#123, all now closed). Removed `blocked` and added `agent-ready` to #124 with an evidence comment summarizing the delegation/linkage/recovery contracts (#121, #122, #123/Plan 0112) it builds inspection, cycle-validation, and the two-agent review on.
- Retrospective state: Sprint 20 has one open issue (#124), so close-or-remediate review is not yet eligible.
- Roadmap horizon: 9 ordered open milestones before and after this run (Sprint 20 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was needed.
- Next eligible action: implement #124 (expose parent-child delegation inspection from both sides, reject self-delegation/cycles at declaration time, and add a committed two-agent interruption review). Final target `main`; after this MEMORY handoff commit, the worktree is clean except for the preserved unrelated untracked `.claude/` directory.
