# Automation Memory

- Run: 2026-07-14T07:10:52Z — implementation run (scheduled).
- Active milestone: Sprint 14 "Capability-based provider routing" (#14). Selected #95 (priority:3), the sole open/`agent-ready` issue, unblocked since #94 closed.
- Completed: `execute_next_step` previously called `routing_policy.resolve(...)` *before* dispatching the step to `RUNNING`, so a `ValueError` from no-eligible-provider resolution propagated out uncaught — the step stayed `QUEUED` forever with no recorded reason, and would have crashed `worker.run_worker`'s poll loop (its `except (ApprovalRequiredError, ContextReferencesUnresolvedError)` clause does not catch a bare `ValueError`). Reachable in practice: a capability valid at step-creation time can have no eligible provider at dispatch under a narrower operator-supplied `--provider-preference` policy. Fixed by catching that `ValueError`, still dispatching the step to `RUNNING` via the existing `start_next_step` path (`provider_route=None`), then failing it through the existing `fail_step_from_error` helper — the same path already used for adapter-call failures after dispatch, so no new state-transition or history code was needed. `RunStep.failure_kind`/`retry_eligible` already classify any failed provider-message step with a string `error`/`error_type` output as `StepFailureKind.DEFINITE`, so retry-eligibility came for free; `worker.run_worker` needed no change since `execute_next_step` now returns a terminal `(step, run)` tuple instead of raising. Also changed `start_next_step`/`transition_step` to derive the `step_started` history entry's `required_capability` from the step's own message rather than only from a resolved `ProviderRoute`, so a failed-routing dispatch still records the requested capability (`resolved_provider`/`resolved_model`/`routing_reason` stay `null`); fixed-provider steps are unaffected. Documented the failure behavior in DEVELOPMENT.md's `run history` section. Added Plan 0091.
- Tests: full suite `607 passed` (up from 604, +3 net): one runtime no-eligible-provider test (explicit reason, `StepFailureKind.DEFINITE`, `retry_eligible is True`, run failure, restart reconstruction), one runtime regression test (fixed-provider step on a separate run unaffected by a capability-routing failure), one CLI end-to-end test forcing the no-eligible-provider path via `--provider-preference`.
- Verification: activated `.venv`; rebuilt and checked the index (24 files, 923 symbols, 5316 relationships); `git diff --check` clean.
- Durable state: implementation commit `e2e351c` pushed to `origin/main`; #95 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search found none. Sprint 14 now has 0 open issues (#93-#95 all closed) — ready for a retrospective run.
- Roadmap horizon: 15 ordered open milestones before and after this run (Sprint 14 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was created.
- Next eligible action: run Sprint 14's retrospective (all issues closed, exit criteria ready for end-to-end review) — do not select a new implementation issue until the retrospective closes or remediates the milestone. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.

---

- Run: 2026-07-14T06:41:13Z — implementation run (scheduled).
- Active milestone: Sprint 14 "Capability-based provider routing" (#14). Selected #94 (priority:2), the sole unblocked `agent-ready` issue; #95 was blocked on it.
- Completed: added `ProviderRoutingPolicy` and `ProviderRoute`, with deterministic first-capable-provider selection from an explicit ordered policy, stable selection reasons, duplicate-policy rejection, model override/default resolution, and a default policy matching `DEFAULT_PROVIDER_SPECS` order. Added read-only `provider routing-policy` inspection plus repeated `--provider-preference` overrides for `run execute-next` and `worker run`. Capability dispatch passes a transient resolved fixed-provider message to the existing adapter boundary while preserving the durable provider-neutral step; fixed-provider messages bypass routing. Extended durable `step_started` history/schema migration with required capability, resolved provider/model, and reason through both queued-run and already-running paths; CLI history omits absent routing fields and never exposes credentials or request bodies. Added Plan 0090 and DEVELOPMENT guidance.
- Tests: full suite `604 passed` (up from 594, +10 net); focused coverage proves deterministic repeatability, policy-order tie-breaking, model override/defaults, restart reconstruction, queued/running history paths, CLI policy inspection and override, redaction, worker compatibility, and fixed-provider bypass.
- Verification: activated `.venv`; rebuilt and checked the index (24 files, 918 symbols, 5275 relationships); `git diff --check` clean.
- Durable state: implementation commit `1edf798` pushed to `origin/main`; #94 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search found only #95. Its sole dependency (#94) is closed, so `blocked` was removed and `agent-ready` added with an evidence comment. Sprint 14 now has one ready issue: #95 (priority:3). The sprint is not eligible for retrospective/close-or-remediate until #95 closes and its no-eligible-provider criterion is reviewed.
- Roadmap horizon: 15 ordered open milestones before and after this run (Sprint 14 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was created.
- Next eligible action: implement #95 (definitive no-eligible-provider failure handling and final fixed-provider regressions) in Sprint 14. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.

---

- Run: 2026-07-14T06:15:00Z — implementation run (scheduled).
- Active milestone: Sprint 14 "Capability-based provider routing" (#14). Selected #93 (priority:1), the sole `agent-ready` issue (created since the prior retrospective run, at 2026-07-14T05:48Z; #94/#95 were `blocked` on it).
- Completed: added `capabilities: tuple[str, ...] = ()` to `ProviderSpec` and declared capabilities on each `DEFAULT_PROVIDER_SPECS` entry (`OPENAI_COMPATIBLE` declares none — unconfigured custom endpoint); `provider list` exposes them via the existing `to_dict()` with no CLI change (DEVELOPMENT.md already documented "declared capabilities" ahead of this landing). Made `ProviderMessage.provider` optional and added `required_capability`, mutually exclusive with it; kept `provider` as the first field with no default so existing positional two-arg call sites (`ProviderMessage("local", "Use it")`) keep their meaning, and appended `required_capability` as a new trailing defaulted field. `RunCoordinator._validate_message` now requires exactly one of `provider`/`required_capability` and rejects an unknown capability against a `_KNOWN_PROVIDER_CAPABILITIES` set derived from `DEFAULT_PROVIDER_SPECS`, before any mutation; `add_step`, restart reconstruction (`_step`), and plan-draft parsing/materialization all share this one validator. Added `run add-step --capability` and updated `PLAN_PROPOSAL_SYSTEM_PROMPT`/DEVELOPMENT.md. Learned there are two independent JSON payload builders for a provider message — the persistence-facing ones in runtime.py filter `None` fields, the CLI-facing ones in cli.py (`_step_payload`, `_plan_step_proposal_payload`) use unfiltered `asdict()` — so `required_capability` always appears (as `null` when unset) in CLI output; updated the two exact-shape test fixtures that assumed six message fields. Confirmed a capability-only step reaching dispatch today fails cleanly (existing `except (ValueError, RuntimeError, NotImplementedError)` in `execute_next_step` catches `ProviderKind(None)`'s `ValueError`) with no new exception handling needed — actual routing resolution is #94's scope. Added Plan 0089.
- Tests: full suite `594 passed` (up from 579, +15 net: `tests/test_providers.py` new; capability/mutual-exclusivity/unknown-capability runtime and CLI cases; plan-proposal parsing/materialization for a capability-routed step; updated `test_cli_adds_and_inspects_provider_message_step` and `PLAN_PROPOSAL_STEPS_PAYLOAD` for the additive field).
- Verification: activated `.venv`; rebuilt index (was stale) and confirmed current (23 files, 886 symbols, 5160 relationships); `git diff --check` clean.
- Durable state: implementation commit `cc46a08` pushed to `origin/main`; #93 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search found #94 and #95, both in Sprint 14. #94's sole stated blocker (#93) is now closed, so `blocked` was removed and `agent-ready` added with an evidence comment. #95 remains correctly `blocked` (depends on open #94). Sprint 14 now has one ready issue: #94 (priority:2).
- Roadmap horizon: 15 ordered open milestones before and after this run (Sprint 14 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was created.
- Next eligible action: implement #94 (deterministic capability routing resolution with durable provenance) in Sprint 14. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.

---

- Run: 2026-07-14T05:32:16Z — retrospective run.
- Active milestone at start: Sprint 13 "Operator-accepted model plan decomposition" (#13). All five implementation issues (#87-#91) were closed, so no implementation issue was selected; created retrospective issue #92.
- Completed: exercised the accepted and rejected operator workflows end to end, reviewed the four milestone exit criteria against Plans 0084-0088, DEVELOPMENT.md, Decisions 0005/0006, committed tests, and current index evidence. All criteria passed: durable provider-proposed drafts preserve malformed evidence and queue nothing before acceptance; `run inspect-plan` is read-only; accept/reject is CAS-safe and atomically materializes all steps or none with plan/agent provenance; accepted command/provider execution and rejected-plan state both reconstruct across fresh process/state connections. #92 records the full retrospective and is closed; Sprint 13 is closed with no remediation milestone.
- Verification: activated `.venv`; focused operator review `2 passed in 0.17s`; full suite `579 passed in 7.01s`; `codex-agentic-os index check` current (23 files, 875 symbols, 5090 relationships). Final `git diff --check` and branch-state checks are part of the handoff commit.
- Durable state: implementation evidence remains product commit `16059a2` on `origin/main`; retrospective issue #92 and milestone #13 were closed in GitHub. This run changes only this durable MEMORY record and commits/pushes it on `main`.
- Blocked review: repository-wide open `blocked` search found none. Resulting active Sprint 14 has 0 open and 0 ready issues.
- Roadmap horizon: 16 ordered open milestones before retrospective closure (Sprint 13 through Sprint 28), 15 after (Sprint 14 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was created.
- Next eligible action: replenishment-only review for Sprint 14 "Capability-based provider routing"; create only issues that close its stated exit-criteria gaps, without implementing one in the same run. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.

---

- Run: 2026-07-13T00:00:00Z — implementation run (scheduled).
- Active milestone: Sprint 13 "Operator-accepted model plan decomposition" (#13). Selected #90 (priority:3), the sole `agent-ready` issue and the milestone's last open issue.
- Completed: no runtime/CLI product code changed — all primitives (`run plan`, `run inspect-plan`, `run accept-plan`/`run reject-plan`, `run execute-next`, `run inspect`, `run history`) already existed from Plans 0084-0087. Closed the milestone's remaining "demonstrate end to end" exit criterion with committed, executable proof: added `test_cli_end_to_end_operator_review_reconstructs_plan_execution_after_restart` (create → plan via fake offline adapter → inspect-plan → pre-acceptance execute-next proves `{"attempted": false}`/no draft step runs before acceptance → accept-plan with a registered agent → two execute-next dispatches through the unchanged `ContainerSandbox`/adapter-resolver worker path → fresh `RunCoordinator`/`StateStore` connections and fresh CLI calls reconstruct objective, accepted draft, both executed steps, terminal `succeeded` status, and the full `created→plan_accepted→run_started→step_started→step_succeeded→step_started→step_succeeded→run_succeeded` history with `plan_id`/`agent_id` provenance) and `test_cli_rejected_plan_remains_reconstructable_with_no_executable_steps_after_restart` (propose → reject → simulated restart → draft still shows `rejected`/`decision_agent_id`, execute-next still reports no attempt, history reconstructs `created→plan_rejected`). Learned `run accept-plan`/`run reject-plan --agent-id` require a registered agent (unlike `run claim`/`run add-step --agent-id`), and accepted steps carry a persisted sandbox policy so `run execute-next` must omit `--sandbox`. Added a DEVELOPMENT.md pointer to both tests instead of duplicating a manual walkthrough. Added Plan 0088.
- Tests: full suite `579 passed` (up from 577, +2 new end-to-end/regression tests).
- Verification: activated `.venv`; rebuilt index and confirmed current (23 files, 875 symbols, 5090 relationships); `git diff --check` clean.
- Durable state: implementation commit `16059a2` pushed to `origin/main`; #90 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search found none. Sprint 13 now has 0 open issues (#87-#91 all closed) — ready for a retrospective run.
- Roadmap horizon: 16 ordered open milestones (Sprint 13 through Sprint 28), above the three-sprint threshold; no planning handoff was needed.
- Next eligible action: run Sprint 13's retrospective (all issues closed, exit criteria ready for end-to-end review) — do not select a new implementation issue until the retrospective closes or remediates the milestone. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.
