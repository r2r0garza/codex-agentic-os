# Automation Memory

- Run: 2026-07-14T10:10:25Z — implementation run (scheduled).
- Active milestone: Sprint 16 "Live run observation" (#16). Selected #101 (priority:1), the sole `agent-ready` issue, unblocked (no stated dependency).
- Completed: added `run watch RUN_ID --interval SECONDS`, a read-only CLI command that polls `RunCoordinator.list_history`/`list_steps` only (no new persistence) and prints each new durable history entry exactly once in sequence order as one compact JSON object per line, tracked by an in-process sequence cursor, until the run reaches a terminal status or the operator interrupts (SIGINT/SIGTERM). Reused the worker loop's shutdown-signal adapter (`_install_worker_shutdown_signals`) so an interrupt stops the loop cleanly at its next poll boundary and restores the process's prior signal disposition, mirroring `worker run`. A step blocked on a pending approval (the same `ApprovalStatus.PENDING`/next-queued-step condition `start_next_step` enforces at dispatch) is surfaced once as a distinct `{"event": "blocked", ...}` notice, not repeated on later polls. `--interval` must be positive, validated before the state database is even checked for existence. Added Plan 0095 and DEVELOPMENT.md guidance.
- Tests: full suite `668 passed` (up from 660, +8 net): ordered/no-duplicate emission across polls with a concurrent mutator, terminal auto-stop, the blocked-approval notice (announced once, not per poll), positive-interval validation, missing-run/missing-database rejection without creating state, non-mutation of run/step state, and clean SIGINT/SIGTERM shutdown with signal-disposition restoration (parametrized over both signals).
- Verification: activated `.venv`; rebuilt and checked the index (24 files, 1006 symbols, 5914 relationships); `git diff --check` clean; live CLI UAT against a real repository-local SQLite database confirmed immediate clean exit on an already-terminal run, correct NDJSON output, and clean rejection of an unknown run and a non-positive interval without creating a database.
- Durable state: implementation commit `0bce819` pushed to `origin/main`; #101 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search found #102 and #103, both in Sprint 16. #102's sole stated blocker (#101) is now closed, so `blocked` was removed and `agent-ready` added with an evidence comment noting it covers an explicit cross-process cursor option (distinct from #101's in-process-only cursor). #103 remains correctly `blocked` (depends on open #101 *and* #102; #102 not yet closed, so no comment was added since its blocker state did not change). Sprint 16 now has one ready issue: #102 (priority:2).
- Roadmap horizon: 13 ordered open milestones before and after this run (Sprint 16 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was created.
- Next eligible action: implement #102 (explicit cross-process `run watch` sequence-cursor resume option) in Sprint 16. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.

---

- Run: 2026-07-14T09:33:32Z — retrospective run (scheduled).
- Active milestone at start: Sprint 15 "Durable step artifacts with provenance" (#15). All three implementation issues (#97-#99) were closed, so no implementation issue was selected; created retrospective issue #100.
- Completed: exercised command-step artifact capture from a mounted workspace, stable redacted listing, byte-identical export, absent-artifact failure, and provider-response listing/export through the operator-facing CLI/runtime paths. All four milestone exit criteria passed. Decisions 0001/0002 remain upheld: provider capture consumes the normalized chat boundary, while command and provider artifacts share one small provider-neutral durable storage contract. #100 records the evidence and is closed; Sprint 15 is closed with no remediation milestone.
- Verification: activated `.venv`; focused operator artifact review `5 passed in 0.19s`; full suite `660 passed in 8.32s`; `codex-agentic-os index check` current (24 files, 993 symbols, 5835 relationships); `git diff --check` clean.
- Durable state: product evidence remains commits `f65018d` (#97), `1958757` (#98), and `1384ec6` (#99), all on `origin/main`; retrospective issue #100 and milestone #15 were closed in GitHub. This run changes only this durable MEMORY record and commits/pushes it on `main`.
- Blocked review: repository-wide open `blocked` search found none. Resulting active Sprint 16 has 0 open and 0 ready issues.
- Roadmap horizon: 14 ordered open milestones before retrospective closure (Sprint 15 through Sprint 28), 13 after (Sprint 16 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was created.
- Next eligible action: replenishment-only review for Sprint 16 "Live run observation"; create only issues that close its stated exit-criteria gaps, without implementing one in the same run. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.

---

- Run: 2026-07-14T09:10:15Z — implementation run (scheduled).
- Active milestone: Sprint 15 "Durable step artifacts with provenance" (#15). Selected #99 (priority:3), the sole open/`agent-ready` issue, unblocked since #97/#98 closed.
- Completed: added `RunCoordinator.read_artifact_content(artifact_id)` — read-only, reuses the existing `_artifact_storage_dir`/`ArtifactStatus` contract, raising `KeyError` for an unknown artifact and a clean `ValueError` for absent, rejected, or locally-missing-despite-captured content. Added `run list-artifacts RUN_ID [--step STEP_ID]`, printing each artifact's run id, step id, name, status, content hash, and size in the existing stable `(step_id, name)` order (reusing Plan 0092's `RunCoordinator.list_artifacts`), and `run export-artifact RUN_ID --name NAME [--step STEP_ID] --destination PATH`, which resolves the named artifact (disambiguated by `--step` when more than one step declares the same name — artifacts are referenced by name, matching the operator-facing `--artifact`/`--response-artifact` vocabulary, not the internal `artifact_id`) and writes its stored content byte-identical to the destination. Both commands are read-only against the state database and never write the destination or mutate state on a missing run, unknown/ambiguous name, or absent/rejected artifact. Added Plan 0094 and DEVELOPMENT.md guidance.
- Tests: full suite `660 passed` (up from 644, +16 net), covering stable listing order across captured/absent/rejected records, redaction (no host path/command/env), `--step` filtering, byte-identical export, every clean-failure path (missing run, unknown name, ambiguous name, absent, rejected/oversized), non-mutation of run/step state, and both command-step and provider-response artifacts end-to-end through the CLI.
- Verification: activated `.venv`; rebuilt and checked the index (24 files, 993 symbols, 5835 relationships); `git diff --check` clean.
- Durable state: implementation commit `1384ec6` pushed to `origin/main`; #99 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search found none. Sprint 15 now has 0 open issues (#97-#99 all closed) — ready for a retrospective run.
- Roadmap horizon: 14 ordered open milestones before and after this run (Sprint 15 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was created.
- Next eligible action: run Sprint 15's retrospective (all issues closed, exit criteria ready for end-to-end review) — do not select a new implementation issue until the retrospective closes or remediates the milestone. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.

---

- Run: 2026-07-14T08:37:10Z — implementation run (scheduled).
- Active milestone: Sprint 15 "Durable step artifacts with provenance" (#15). Selected #98 (priority:2), the sole unblocked `agent-ready` issue; #99 was blocked on it.
- Completed: added `RunStep.response_artifact_name`, `RunCoordinator.add_step(..., response_artifact_name=...)`, and `run add-step --response-artifact NAME`, validated before mutation and persisted across restart and every lifecycle payload rewrite. On a successful provider response, normalized `response.content` is UTF-8 encoded and captured through the same shared artifact persistence helper used by command files: local content storage, sha256, byte size, run/step/name identity, configured size-limit rejection, read-only inspection, and durable history with `execution_kind=provider`; rejected oversize content does not change the provider step's success, while adapter/routing failures capture nothing. Existing normalized output (including raw envelope), usage evidence, context resolution, capability routing, and fixed-provider behavior remain compatible. Added Plan 0093 and DEVELOPMENT guidance.
- Tests: full suite `644 passed` (up from 634, +10 net), covering declaration validation/no mutation, restart round-trip, command rejection, shared capture/hash/bytes, context + capability routing + usage compatibility on a non-final step, redacted provider history, size rejection without step failure, no capture on adapter failure, and CLI creation/inspection/rejection.
- Verification: activated `.venv`; rebuilt and checked the index (24 files, 972 symbols, 5631 relationships); `git diff --check` clean.
- Durable state: implementation commit `1958757` pushed to `origin/main`; #98 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search found #99 in Sprint 15. Its dependencies #97/#98 are now closed, so `blocked` was removed and `agent-ready` added with an evidence comment. Sprint 15 now has one open/ready issue: #99 (priority:3). The sprint is not yet retrospective-eligible because #99 remains open.
- Roadmap horizon: 14 ordered open milestones before and after this run (Sprint 15 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was created.
- Next eligible action: implement #99 (stable read-only artifact listing and byte-identical export for command/provider artifacts), then run Sprint 15's retrospective in a later run after all milestone issues close. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.

---

- Run: 2026-07-14T08:21:33Z — implementation run (scheduled).
- Active milestone: Sprint 15 "Durable step artifacts with provenance" (#15). Selected #97 (priority:1), the sole open/`agent-ready` issue, unblocked (#98/#99 were `blocked` on it).
- Completed: added `ArtifactDeclaration`/`ArtifactRecord` and a new durable `artifact` state kind (SQLite `state_records`, migrated in place like the existing `step` kind), plus content bytes written to a local `<state-db-dir>/artifacts/` directory. `run add-step --artifact NAME=PATH` (and `RunCoordinator.add_step(..., artifacts=...)`) declares named artifact paths on a command step, validated before mutation: requires a persisted sandbox policy with at least one mount, and the declared path must resolve within a mount's container-side path (rejecting escapes and paths outside every mount). After a successful command result (`exit_code` zero), `complete_step_from_result` resolves each declared container path to its host-side path through the persisted mounts and captures present files (sha256 hash + size) into artifact storage; a missing file is recorded `absent`; a file over the configured limit (default 10,000,000 bytes, overridable via `RunCoordinator(artifact_size_limit_bytes=...)`) is recorded `rejected` without reading its content — neither changes the step's own success/failure outcome. Capture/absence/rejection are recorded in durable run history via a new `artifact_name` column, under the existing redaction contract. `run inspect`/`run inspect-step` show `artifact_declarations` and, once captured, a redacted `artifacts` list (name, status, hash, size — never the resolved host path). While threading artifact preservation through every existing step-payload rewrite site (the Plan 0072 contract), found and fixed a pre-existing gap: `RunCoordinator.transition_step` never persisted `sandbox_policy` when dispatching a command step in a run already `RUNNING` (i.e. any command step after the first) — this would have silently broken both sandbox reproducibility and artifact capture on that path; covered by a dedicated multi-step regression test. Added Plan 0092 and DEVELOPMENT.md guidance.
- Tests: full suite `634 passed` (up from 607, +27 net): declaration validation (missing sandbox policy, no mounts, path escape/outside-mount, name charset/uniqueness/unknown-fields), successful capture with hash/size, missing-artifact absence, size-limit rejection without streaming, no capture on command failure, redacted history entries, multi-step dispatch preservation, restart round-trip, CLI queuing/inspection/rejection, and one CLI `execute-next` end-to-end capture test via a monkeypatched `ContainerSandbox.execute` that writes into the mounted host directory.
- Verification: activated `.venv`; rebuilt and checked the index (24 files, 953 symbols, 5539 relationships); `git diff --check` clean.
- Durable state: implementation commit `f65018d` pushed to `origin/main`; #97 closed with commit and verification evidence.
- Blocked review: repository-wide open `blocked` search found #98 and #99, both in Sprint 15. #98's sole stated blocker (#97) is now closed, so `blocked` was removed and `agent-ready` added with an evidence comment. #99 remains correctly `blocked` (depends on open #97 and #98; #98 not yet closed). Sprint 15 now has one ready issue: #98 (priority:2).
- Roadmap horizon: 14 ordered open milestones before and after this run (Sprint 15 through Sprint 28), above the three-sprint threshold; no planning handoff or roadmap issue was created.
- Next eligible action: implement #98 (persist provider-message responses as durable artifacts through the same storage contract) in Sprint 15. Final target `main`; worktree dirty only for this MEMORY update before its durable handoff commit.
