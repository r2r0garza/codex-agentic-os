# Automation Memory

- Run: 2026-07-13T19:15:00Z — implementation run.
- Active milestone: Sprint 11 "Explicit cross-step provider context" (#11). Selected the sole unblocked `agent-ready` issue, #80 (map resolved context into native provider message payloads, priority:3), the final open issue in Sprint 11.
- Completed: `execute_next_step` now maps a provider step's resolved `context_step_ids` into explicit ordered prior-context messages instead of ignoring them at dispatch. For each declared reference, in declared order, it replays a `(user, assistant)` pair reconstructed from the referenced step's objective (user turn) and its durable output (assistant turn) — a provider step's persisted response `content`, or a rendered `exit_code=…\nstdout:\n…\nstderr:\n…` for a command step — placed after any system instruction and before the step's own current user message. No adapter code changed: `chat.py`'s `OpenAICompatibleAdapter`/`AnthropicAdapter`/`GoogleAdapter` already mapped an arbitrary ordered `ChatMessage` tuple into each provider family's native shape (`messages`, `messages`+`system`, `contents`+`systemInstruction`); the pairing was chosen specifically so the sequence always keeps Anthropic's strict user/assistant alternation valid. A provider step with no context references keeps today's exact single-turn payload (empty context tuple). Added Plan 0079 and a DEVELOPMENT.md note describing the mapping.
- Verification: activated `.venv`; focused `tests/test_chat.py` + `tests/test_runtime.py` suites added 5 new tests (OpenAI-compatible/Anthropic/Google multi-turn context payload mapping, an OpenAI-compatible no-reference regression payload, and a runtime dispatch test mixing a command-step and a provider-step reference), 183 passed; full suite `499 passed` (up from 494); a fresh-process CLI/SQLite UAT created a run with a command step, a provider step, and a synthesis step declaring both as context via the CLI in one process, dispatched all three via `execute_next_step`'s public `executor`/`adapter_resolver` injection points in a second process, confirmed the synthesis adapter received messages in the exact expected order, and confirmed the run and all steps read back `succeeded` via the CLI in a third process; committed index rebuilt/current (20 files, 715 symbols, 4233 relationships); `git diff --check` clean.
- Durable state: implementation commit `f3d682c` pushed to `origin/main`; issue #80 auto-closed via its commit trailer and received a verification comment. This MEMORY finalization commit/push is pending.
- Blocked review: repository-wide `blocked`-label search found 0 open issues. Sprint 11 now has 0 open issues (all of #78, #79, #80 closed), so the next eligible run is a retrospective against Sprint 11's exit criteria; do not implement during that run.
- Roadmap horizon: 18 open milestones before and after this run (Sprint 11 through Sprint 28), above the three-sprint threshold, so no planning handoff was needed.
- Final target: `main`; worktree dirty only for this MEMORY finalization before commit.

---

- Run: 2026-07-13T18:17:06Z — implementation run.
- Active milestone: Sprint 11 "Explicit cross-step provider context" (#11). Selected the sole unblocked `agent-ready` issue, #79 (resolve provider context references at dispatch eligibility, priority:2). Sprint 11 retains open issue #80, so its exit criteria are not ready for retrospective or close-or-remediate review.
- Completed: `start_next_step` (and therefore `execute_next_step`) now resolves a provider-message step's declared `context_step_ids` against current durable step status immediately before dispatch, using fresh `list_steps` lookups rather than any value cached at step declaration. A step with any reference not yet `succeeded` stays queued and ineligible: a new `ContextReferencesUnresolvedError` (`ValueError` subclass, exported from the package root) is raised without mutating run or step state, mirroring the approval gate's queued-but-ineligible semantics; the CLI's existing generic error handling reports this deterministically (exit code 2). Once every reference has succeeded, dispatch proceeds normally and the durable `step_started` history entry now carries the resolved `context_step_ids` in declared order (ids only, no referenced output) via a new `run_history.context_step_ids` column threaded through `StateStore.transition_step`/`put_many`. Existing approval-gated and command-step dispatch behavior is unchanged. Added Plan 0078 and a DEVELOPMENT.md note on the dispatch gate and the new history field.
- Verification: activated `.venv`; focused runtime/state/CLI suites added 8 new tests (successful dispatch with history evidence, cancelled/failed unresolved references without mutation, dispatch-time freshness, approval-gate composition, deterministic CLI ineligible-gate error without mutation, history persistence of `context_step_ids`), all passing; full suite `494 passed` (up from 486); a fresh-process CLI/SQLite UAT confirmed a cancelled-reference step reports the ineligible gate deterministically (exit code 2) with zero state mutation, and confirmed a real dispatch after the reference succeeded records `context_step_ids` on the `step_started` history entry; committed index rebuilt/current (20 files, 697 symbols, 4179 relationships); `git diff --check` clean.
- Durable state: implementation commit `ddb6abc` pushed to `origin/main`; issue #79 auto-closed via its commit trailer and received a verification comment.
- Blocked review: repository-wide review found only #80, blocked on #79. #79 is now closed, so `blocked` was removed and `agent-ready` added to #80 with an evidence comment. Sprint 11 now has one ready issue: #80 (priority:3), the next eligible implementation.
- Roadmap horizon: 18 open milestones before and after this run (Sprint 11 through Sprint 28), above the three-sprint threshold, so no planning handoff was needed.
- Final target: `main`; worktree dirty only for this MEMORY finalization before commit.

---

- Run: 2026-07-13T17:36:18Z — implementation run.
- Active milestone: Sprint 11 "Explicit cross-step provider context" (#11). Selected the sole unblocked `agent-ready` issue, #78 (persist explicit provider context step references, priority:1). Sprint 11 retains open issues #79 and #80, so its exit criteria are not ready for retrospective or close-or-remediate review.
- Completed: provider-message steps can now durably declare ordered earlier same-run references through `RunStep.context_step_ids`, the library `add_step(..., context_step_ids=...)` boundary, and repeatable CLI `run add-step --context-step STEP_ID`. Unknown references, cross-run references, duplicates, and command-step context reject before append. References survive restart, every lifecycle payload rewrite, approval decisions, and explicit retry attempts. Read-only `run inspect` / `run inspect-step` show step ids only and never inline referenced outputs. Existing steps without references retain their prior inspection shape. Added Plan 0077 and DEVELOPMENT guidance.
- Verification: activated `.venv`; focused runtime/CLI/state suites passed (`374 passed`, including 8 new tests); full suite `486 passed` (up from 478); a fresh-process CLI/SQLite UAT persisted and reloaded two references in declared order and confirmed the durable provider-step payload contains ids but no copied referenced output; committed index rebuilt/current (20 files, 682 symbols, 4081 relationships); `git diff --check` clean.
- Durable state: implementation commit `eac3294` pushed to `origin/main`; issue #78 auto-closed via its commit trailer and received a verification comment. This MEMORY finalization commit/push is pending.
- Blocked review: repository-wide review found #79 and #80. #79's sole dependency #78 is closed, so `blocked` was removed and `agent-ready` added with an evidence comment. #80 remains correctly blocked on open #79. Sprint 11 now has one ready issue: #79 (priority:2), the next eligible implementation.
- Roadmap horizon: 18 open milestones before and after this run (Sprint 11 through Sprint 28), above the three-sprint threshold, so no planning handoff is needed.
- Final target: `main`; worktree dirty only for this MEMORY finalization before commit.

---

- Run: 2026-07-13T17:10:49Z — implementation run with post-implementation retrospective and milestone closure.
- Active milestone at start: Sprint 10 "Provider usage and cost evidence" (#10), holding its sole unblocked `agent-ready` issue, #76 (add read-only `run usage` CLI summary, priority:3). Implementation issue #76 and retrospective issue #77 are now closed, and milestone #10 is closed without remediation. Sprint 11 "Explicit cross-step provider context" (#11) is active with 0 issues.
- Completed: added `codex-agentic-os run usage RUN_ID`, a read-only command listing provider steps in durable position order (step id, status, provider, resolved model, and a `usage` block of `available`/`input_tokens`/`output_tokens`/`raw`/`unavailable_reason`) plus a run-level aggregate (`steps_with_usage_available`, `steps_with_usage_unavailable`, summed `input_tokens`/`output_tokens`, null when nothing is available). Command steps are omitted from the list; steps that have not yet succeeded or whose adapter reported no usage are marked explicitly unavailable rather than fabricating zero counts. The command reuses the existing read-only `StateStore` path and missing-database/unknown-run rejection shared with `inspect`/`history`/`approvals`/`staleness`. Added Plan 0076 and a DEVELOPMENT.md section for the command.
- Verification: activated `.venv`; focused CLI suite added 6 new tests (stable ordering/aggregation, unavailable-usage non-fabrication, command-only runs, unknown run, missing database, no-mutation read-only behavior), all passing; full suite `478 passed` (up from 472); a three-process SQLite UAT registered an agent, completed a provider step with usage in a second process, and read the aggregate back from a third process with no credential/system-prompt/request-content leakage and an unchanged step revision after the read; committed index rebuilt/current (20 files, 674 symbols, 4021 relationships); `git diff --check` clean. Retrospective #77 recorded all four Sprint 10 exit criteria passing (durable usage persistence, explicit unavailable degradation, the read-only CLI summary, and offline per-adapter-family usage tests) with no known defect or remediation need.
- Durable state: implementation commit `f924de8` pushed to `origin/main`; issue #76 auto-closed via `Closes #76` trailer and received a verification comment; retrospective #77 created, completed, and closed; milestone #10 closed. This MEMORY entry's durable commit/push is pending.
- Blocked review: repo-wide `blocked`-label search found 0 open issues. Sprint 11 has 0 ready issues, so the next eligible run is replenishment against its exit criteria; do not implement during that run.
- Roadmap horizon: 19 open milestones before Sprint 10 closure and 18 after (Sprint 11 through Sprint 28), above the three-sprint threshold, so no planning handoff occurred and no milestones were added.
- Final target: `main`; worktree dirty only for this MEMORY update before commit.
